# توثيق مشروع Shoto

هاد الملف شرح كامل لكود Shoto: شو المفاهيم الأساسية المستخدمة، شو بيعمل كل جزء من الكود، وليش اتخذنا كل قرار تقني. الهدف إنك تقدر تفتح أي ملف بالمشروع وتفهم ليش هو مكتوب هيك.

> **ملاحظة:** الكود نفسه بالإنجليزي (زي أي مشروع Flutter احترافي)، بس الشرح هون بالعربي عشان يكون أوضح إلك.

---

## جدول المحتويات

1. [نظرة عامة](#1-نظرة-عامة)
2. [المفاهيم الأساسية يلي لازم تفهمها](#2-المفاهيم-الأساسية-يلي-لازم-تفهمها)
3. [بنية المجلدات](#3-بنية-المجلدات)
4. [شرح كل Feature بالتفصيل](#4-شرح-كل-feature-بالتفصيل)
5. [نظام التصميم (Design System)](#5-نظام-التصميم-design-system)
6. [قاعدة البيانات المحلية (sqflite)](#6-قاعدة-البيانات-المحلية-sqflite)
7. [مشاكل حقيقية واجهناها وكيف انحلت](#7-مشاكل-حقيقية-واجهناها-وكيف-انحلت)
8. [شو لسا ناقص / خطوات جاية](#8-شو-لسا-ناقص--خطوات-جاية)

---

## 1. نظرة عامة

**Shoto** تطبيق Flutter بيساعد المستخدم ينظّم السكرين شوتات تبعته: بيجيبهم أوتوماتيكياً من الجاليري، بيخليه يحطهم بفولدرز، يفضّل المهم منهم، ويستقبل صور مشارَكة من تطبيقات تانية مباشرة.

**التقنيات المستخدمة:**

| الغرض | المكتبة |
|---|---|
| إدارة الحالة (State Management) | `flutter_bloc` |
| حقن الاعتماديات (Dependency Injection) | `get_it` |
| التوجيه بين الشاشات (Routing) | `go_router` |
| تحجيم متجاوب مع كل الشاشات | `flutter_screenutil` |
| تسجيل الدخول | `firebase_auth` + `google_sign_in` + `sign_in_with_apple` |
| الوصول لصور الجهاز | `photo_manager` |
| قاعدة بيانات محلية (فولدرز/مفضّلة) | `sqflite` |
| استقبال المشاركة من تطبيقات تانية | `receive_sharing_intent` |
| مشاركة صورة لتطبيقات تانية | `share_plus` |
| الخطوط | `google_fonts` |

**قرار مهم بالمشروع:** كل شي محلي (`local-first`) — التطبيق ما بيرفع أي صورة عالسيرفر. الحساب (Firebase Auth) مستخدم بس لتسجيل الدخول، مش لتخزين الصور. الصور نفسها تضل بجاليري الجهاز، وإحنا بس بنخزن "بيانات تنظيم" (فولدر، مفضّلة) بقاعدة بيانات محلية.

---

## 2. المفاهيم الأساسية يلي لازم تفهمها

### 2.1 Widget و `build()`

بفلاتر، كل شي عالشاشة هو **Widget**. كل Widget عنده method اسمها `build(BuildContext context)` بترجع Widgets تانية — وهيك بتتبنى "شجرة" (widget tree). فلاتر بيعيد استدعاء `build()` كل مرة الحالة (state) بتتغير، وبيقارن الشجرة الجديدة بالقديمة ويحدّث بس اللي تغيّر فعلياً عالشاشة (مش بيعيد رسم كل شي من الصفر).

- **`StatelessWidget`** → widget ما عنده حالة داخلية بتتغير (متل `PrimaryButton`, `ShotoLogo`, `FolderCard`). بيتبنى مرة وخلص.
- **`StatefulWidget`** → widget عنده حالة بتتغير بمرور الوقت (متل `MainShellPage` يلي بيتتبع أي تاب مفتوح حالياً، أو `ScreenshotDetailPage` يلي بيتتبع أي صورة معروضة). بيتكون من جزئين: الـ `Widget` نفسه (بيانات ثابتة مثل `initialIndex`) و`State` (بيانات متغيرة). لما تنادي `setState(() { ... })`، فلاتر بيعيد بناء الـ widget.

### 2.2 `async` / `await` / `Future`

معظم عمليات الكود (قراءة من الجاليري، الكتابة لقاعدة البيانات، تسجيل الدخول) عمليات **غير متزامنة (asynchronous)** — يعني بتاخد وقت وما لازم توقف كل التطبيق لحد ما تخلص.

```dart
Future<List<AssetEntity>> getScreenshotAssets() async {
  final assets = await album.getAssetListRange(start: 0, end: count);
  return assets;
}
```

- `Future<T>` = "وعد" إنو بعد شوي رح يوصلك قيمة نوعها `T`.
- `async` بتخلي الـ function تقدر تستخدم `await`.
- `await` بتوقف تنفيذ **هاي الـ function بس** (مش كل التطبيق) لحد ما الـ `Future` يخلص، وبعدين تكمل.

### 2.3 `Stream`

`Stream` زي `Future` بس بترجع كذا قيمة عبر الوقت مش قيمة وحدة. مستخدمة بالمشروع بمكانين مهمين:
- `FirebaseAuth.authStateChanges()` → بترسل حدث كل ما تسجيل الدخول يتغيّر (دخول/خروج).
- `PhotoManager` عنده callback بيراقب تغييرات الجاليري (صورة انضافت/انحذفت)، وإحنا لفّيناه بـ `Stream` بملف [`screenshot_gallery_data_source.dart`](lib/features/screenshots/data/data_sources/screenshot_gallery_data_source.dart).

### 2.4 Clean Architecture — ليش كل Feature مقسوم 3 طبقات؟

كل مجلد جوا `lib/features/<اسم الفيتشر>` مقسوم دايماً لـ 3 طبقات:

```
features/<feature>/
  domain/          ← "شو" التطبيق بيعمل (منطق العمل الخام، بدون تفاصيل تقنية)
    entities/      ← نماذج البيانات النظيفة (مثلاً UserEntity، FolderEntity)
    repositories/  ← "عقود" (abstract class) بتحدد شو ممكن تعمل، بدون تفاصيل التنفيذ
    use_cases/     ← عملية وحدة محددة (مثلاً SignInWithGoogleUseCase)
  data/            ← "كيف" بيصير التنفيذ فعلياً (Firebase، photo_manager، sqflite...)
    data_sources/  ← الكود يلي فعلياً بيحكي مع الخدمة الخارجية (API، قاعدة بيانات، SDK)
    models/        ← نسخة من الـ Entity بس فيها كود التحويل من/لـ JSON أو DB row
    repositories_impl/ ← التنفيذ الفعلي لعقود domain/repositories
  presentation/    ← الشاشات والـ UI
    bloc/          ← إدارة الحالة (event/state/bloc)
    pages/          ← الشاشات نفسها
    widgets/        ← ويدجيتس خاصة بهاد الفيتشر بس
```

**ليش هيك؟** لأنو `domain` (المنطق) ما بيعرف **شو** التقنية المستخدمة بالـ `data` — مثلاً `AuthRepository` (بـ domain) بس بيقول "فيه `signInWithGoogle()` بيرجع `UserEntity`"، بدون ما يعرف إنو ورا الكواليس فيه Firebase أو GoogleSignIn. هيك لو بكرة بدك تبدل Firebase بشي تاني، بس `AuthRepositoryImpl` (بـ data) لازم يتغيّر — الـ `presentation` والـ `domain` ما بيحسّوا بالفرق أبداً.

**مثال حقيقي من المشروع (فيتشر Auth):**

```
domain/repositories/auth_repository.dart   → "عقد": Future<UserEntity> signInWithGoogle();
data/repositories_impl/auth_repository_impl.dart → التنفيذ الفعلي، بينادي AuthRemoteDataSource
data/data_sources/auth_remote_data_source.dart   → هون فعلياً بنحكي مع Firebase و GoogleSignIn
```

### 2.5 نمط BLoC — كيف الحالة بتتحدث بالشاشة

`flutter_bloc` هو نمط لإدارة الحالة مبني على 3 أشياء:

- **Event** → "شو صار" (مثلاً المستخدم دوس زر تسجيل الدخول بجوجل → `SignInWithGoogleEvent`)
- **Bloc** → "الدماغ": بياخد Event، بينفذ المنطق (بينادي use case مثلاً)، وبيطلع State جديدة
- **State** → "شو الشاشة لازم تعرض هلأ" (تحميل، نجاح، خطأ...)

```dart
// auth_event.dart
class SignInWithGoogleEvent extends AuthEvent {}

// auth_bloc.dart
on<SignInWithGoogleEvent>((event, emit) async {
  emit(AuthLoadingState(AuthMethod.google));   // 1. قول للشاشة "إحنا عم نحمّل"
  try {
    final user = await signInWithGoogleUseCase();
    emit(AuthSuccessState(user));               // 2. نجح! حدّث الشاشة
  } catch (error) {
    emit(AuthErrorState(_mapError(error)));     // 3. فشل! اعرض رسالة خطأ
  }
});
```

بالـ UI، الشاشة "بتستمع" لهاي الـ States عبر `BlocBuilder` (بس تبني الواجهة حسب الحالة) أو `BlocListener`/`BlocConsumer` (بتاخد فعل زي navigation أو snackbar بدون ما تعيد بناء الواجهة).

**ملاحظة مهمة عن أسلوب المشروع:** الـ Event والـ State هون كلاسات عادية (مش `Equatable`، بدون مقارنة تلقائية) — هيك اخترنا نمشي عليه بكل الفيتشرز حفاظاً على الاتساق.

### 2.6 حقن الاعتماديات (Dependency Injection) — `get_it`

بدل ما كل صفحة تنشئ الأشياء يلي بتحتاجها بنفسها (متل `AuthRepositoryImpl(AuthRemoteDataSource())`)، فيه "صندوق" واحد اسمه `sl` (اختصار Service Locator) بملف [`dependency_injection.dart`](lib/core/di/dependency_injection.dart) بيسجل فيه كل شي مرة وحدة، وبعدين أي مكان بالكود يقدر يطلب `sl<AuthRepository>()` وياخد نسخة جاهزة.

```dart
sl.registerLazySingleton<AuthRepository>(() => AuthRepositoryImpl(sl()));
```

فيه نوعين تسجيل مستخدمين بالمشروع:
- **`registerLazySingleton`** → نسخة واحدة بس، بتتعمل أول مرة حدا يطلبها، وبعدين نفس النسخة كل مرة (مستخدم للـ repositories والـ use cases، يعني ما في داعي نسخة جديدة كل مرة).
- **`registerFactory`** → نسخة **جديدة** كل مرة حدا يطلبها (مستخدم للـ Blocs، لأنو كل شاشة لازم يكون عندها Bloc خاص فيها، مش تشارك واحد مع شاشة تانية).

### 2.7 التوجيه (Routing) — `go_router` وحراسة تسجيل الدخول

كل الشاشات معرّفة بمكان واحد: [`app_router.dart`](lib/core/routes/app_router.dart). أهم جزء فيه هو الـ `redirect`:

```dart
redirect: (context, state) {
  final bool isLoggedIn = _authRepository.currentUser != null;
  final bool isPreAuthRoute = location == '/onboarding' || location == '/auth';

  if (isLoggedIn && isPreAuthRoute) return '/home';   // مسجل دخول وواقف بصفحة تسجيل؟ وديه عالهوم
  if (!isLoggedIn && location == '/home') return '/auth'; // مش مسجل وحاول يدخل هوم؟ رجّعه لتسجيل الدخول
  return null; // ولا شي، خليه وين ما هو
}
```

وعشان هاد الفحص يعاد تلقائياً كل ما تسجيل الدخول يتغيّر (بدون ما تحتاج hot restart)، فيه `GoRouterRefreshStream` بملف [`go_router_refresh_stream.dart`](lib/core/routes/go_router_refresh_stream.dart) بيحوّل `Stream` (حالة تسجيل الدخول من Firebase) لشي اسمه `Listenable` يفهمه `go_router`.

### 2.8 وين بتنخزن كل بيانة؟

هاي نقطة كتير مهمة تتفهمها بهاد المشروع:

| البيانة | وين مخزّنة | ليش |
|---|---|---|
| الصورة نفسها (bytes) | جاليري الجهاز (نظام التشغيل) | إحنا ما بنخزن صور، بس بنأشر عليها عبر `photo_manager` |
| الفولدرز، أي صورة بأي فولدر، المفضّلة | `sqflite` (قاعدة بيانات محلية بملف عالجهاز) | بيانات "تنظيم" خاصة فينا، مش موجودة أصلاً بنظام التشغيل |
| حساب المستخدم (اسم، إيميل، صورة بروفايل) | Firebase Auth (سحابي) | تسجيل الدخول لازم يكون موثّق ومتزامن |

---

## 3. بنية المجلدات

```
lib/
  core/                          ← أشياء مشتركة بين كل الفيتشرز
    constants/                   ← قيم ثابتة (auth_constants.dart)
    database/                    ← AppDatabase (اتصال sqflite واحد مشترك)
    di/                          ← dependency_injection.dart
    routes/                      ← app_router.dart + go_router_refresh_stream.dart
    theme/                       ← app_colors / app_text_styles / app_theme
    utils/                       ← nonce_generator.dart (لتسجيل دخول أبل)
    widgets/                     ← ويدجيتس مشتركة بكل التطبيق (PrimaryButton، ShotoLogo...)
  features/
    onboarding/                  ← شاشة الترحيب (قبل تسجيل الدخول)
    auth/                        ← تسجيل الدخول بجوجل/أبل
    screenshots/                 ← جلب السكرين شوتات + عرضها + التفاصيل
    folders/                     ← الفولدرز
    settings/                    ← البروفايل وتسجيل الخروج
    home/                        ← شاشة Home (تعرض ScreenshotsBody)
    shell/                       ← MainShellPage (الـ bottom nav يلي بيربط كل شي)
  main.dart                      ← نقطة انطلاق التطبيق
```

---

## 4. شرح كل Feature بالتفصيل

### 4.1 Onboarding

- [`onboarding_page.dart`](lib/features/onboarding/presentation/pages/onboarding_page.dart): شاشة فيها Masonry grid (شبكة بأحجام متفاوتة) من صور تسكرول لحالها أوتوماتيكياً (`Timer.periodic` بيحرّك الـ `ScrollController`)، وفوقها زر "Get Started" بيوديك عالـ Auth.
- البيانات (الصور والنصوص) جايين من ملف JSON محلي `assets/cfg/onboarding_data.json` — مش hardcoded بالكود، عشان تقدر تغيّرهم بسهولة.

### 4.2 Auth

**الفكرة:** المستخدم بس عندو خيارين: Google أو Apple. ما في تسجيل بإيميل/باسورد.

- [`auth_remote_data_source.dart`](lib/features/auth/data/data_sources/auth_remote_data_source.dart): هون فعلياً بنحكي مع الخدمات الخارجية:
  - **Google**: بيستخدم `google_sign_in` (النسخة الجديدة كتير، API تبعها `GoogleSignIn.instance.authenticate()` مش القديمة `signIn()`)، وبياخد `idToken` منها، وبيبنيه لـ credential فايربيس (`GoogleAuthProvider.credential`).
  - **Apple**: بيستخدم `sign_in_with_apple`. لازم "nonce" (رقم عشوائي مشفّر) لأسباب أمنية — أبل بترجعلك الـ nonce الأصلي متل ما هو، وفايربيس بيتأكد إنو نفسه يلي أرسلناه (موجود بـ [`nonce_generator.dart`](lib/core/utils/nonce_generator.dart)).
  - زر أبل بيظهر بس على iOS/macOS (مش أندرويد) — لأنو تفعيله على أندرويد بيحتاج سيرفر redirect منفصل، مو موجود بالمشروع.
- **`AuthConstants.googleServerClientId`** ([`auth_constants.dart`](lib/core/constants/auth_constants.dart)): لازم يكون فيه الـ "Web client ID" الحقيقي من Firebase Console — من دونه Google Sign-In بيفشل بأخطاء غامضة (صار معنا هيك فعلياً هالجلسة).
- [`auth_page.dart`](lib/features/auth/presentation/pages/auth_page.dart): الشاشة نفسها. بتستخدم `BlocConsumer` — الـ `listener` بيتفاعل مع `AuthSuccessState` وينقلك لـ `/home`، والـ `builder` بيرسم الأزرار حسب حالة التحميل.

### 4.3 Screenshots — قلب التطبيق

هاي أعقد فيتشر، خلينا نفصّل:

- [`screenshot_gallery_data_source.dart`](lib/features/screenshots/data/data_sources/screenshot_gallery_data_source.dart):
  - "سكرين شوت" مش مفهوم رسمي بنظام التشغيل! إحنا بندور على أي ألبوم بالجاليري اسمه فيه كلمة "screenshot" (أندرويد وiOS الاثنين بيسموا الألبوم الرسمي "Screenshots" فعلياً).
  - كمان بندور على ألبوم اسمه بالظبط `"Shoto"` — هاد الألبوم يلي إحنا منعمله لما توصل صورة عبر "مشاركة" من تطبيق تاني (`saveSharedImage`).
  - `getScreenshotAssets()` بيجمع الصور من الألبومين مع بعض (بدون تكرار)، ومرتبين حسب تاريخ الإنشاء.
  - فيه `Stream<void> get changes` — بيراقب تغييرات الجاليري (صورة جديدة انضافت) عبر `PhotoManager.addChangeCallback`، وبيحوّلها لـ `Stream` عادي يقدر الـ Bloc يستمع له.
- [`screenshot_metadata_local_data_source.dart`](lib/features/screenshots/data/data_sources/screenshot_metadata_local_data_source.dart): هون بنتعامل مع `sqflite` — تخزين "مفضّلة؟" و"بأي فولدر؟" لكل صورة (بالـ id تبعها بالجاليري).
- [`screenshot_repository_impl.dart`](lib/features/screenshots/data/repositories_impl/screenshot_repository_impl.dart): بيجمع الاثنين فوق مع بعض — بيجيب الصور من الجاليري، وبيربطها مع بيانات المفضّلة/الفولدر من `sqflite`.
- [`screenshots_bloc.dart`](lib/features/screenshots/presentation/bloc/screenshots_bloc.dart): أهم شي فيه إنو **نفس الـ Bloc** مستخدم لشاشتين مختلفتين:
  - Home (كل الصور): `LoadScreenshotsEvent()`
  - داخل فولدر معيّن: `LoadScreenshotsEvent(folderId: 5)`
  
  بيتذكر أي وضع هو فيه عبر متغير داخلي `_folderId`، وأي تحديث بعدين (حذف، تفضيل، نقل) بيراعي هاد الوضع.
- [`screenshots_body.dart`](lib/features/screenshots/presentation/widgets/screenshots_body.dart): الواجهة المشتركة (Grid + شريط التحديد المتعدد + فلتر المفضّلة) — نفسها مستخدمة بـ Home وبصفحة تفاصيل الفولدر، عشان ما نكرر كود.
- [`screenshot_detail_page.dart`](lib/features/screenshots/presentation/pages/screenshot_detail_page.dart): عرض الصورة full screen مع `PageView` (تسحب يمين/شمال بين الصور)، وشريط أزرار سفلي بأيقونة + كتابة (Favorite / Share / Move / Delete) بدل أيقونات صغيرة فوق مزحومة.
- [`screenshot_actions.dart`](lib/features/screenshots/presentation/widgets/screenshot_actions.dart): مكان واحد لكل "أفعال" الصورة المشتركة بين أكتر من شاشة — دالة `shareScreenshot()` (مستخدمة من شاشة التفاصيل والقائمة السريعة)، ودالة `showScreenshotQuickActionsSheet()` يلي بتفتحها لما تدوس زر "⋯" الظاهر دايماً بزاوية كل صورة بالـ Grid (مش لازم تعرف عن الضغط المطوّل عشان توصلها).
- شريط التحديد المتعدد (`_SelectionToolbar` بملف `screenshots_body.dart`) فيه زر **"Select all"** يحدد كل الصور الظاهرة حالياً (بيراعي فلتر المفضّلة لو مفعّل).
- [`share_intent_listener.dart`](lib/features/screenshots/presentation/widgets/share_intent_listener.dart): widget "غير مرئي" ملفوف حوالين شاشة التطبيق الرئيسية، بيستمع لـ `receive_sharing_intent` (صور جاية من تطبيقات تانية عبر "مشاركة"). لما توصل صورة: بيحفظها بألبوم "Shoto"، وبعدين يفتح شيت يسأل "بأي فولدر بدك تحطها؟".

### 4.4 Folders

- قاعدة بيانات بسيطة: جدول `folders` (اسم، لون، تاريخ إنشاء).
- [`folders_bloc.dart`](lib/features/folders/presentation/bloc/folders_bloc.dart): تحميل/إنشاء/تسمية/حذف فولدر.
- [`move_to_folder_sheet.dart`](lib/features/folders/presentation/widgets/move_to_folder_sheet.dart): شيت (bottom sheet) بيعرض كل الفولدرز، مستخدم بـ 3 أماكن مختلفة: شريط التحديد المتعدد بـ Home، صفحة تفاصيل الصورة، وبعد استيراد صورة من المشاركة — نفس الويدجت بس بعنوان مختلف حسب السياق.
- **نقطة تعلّمناها:** `FoldersBloc` لازم يتعمل مرة وحدة بمستوى `MainShellPage` (مش جوا `FoldersPage` نفسها) — لأنو `IndexedStack` (يلي بيخلي التابات محفوظة بالذاكرة عشان التنقل السريع) ما بيعيد بناء `FoldersPage` أبداً بعد أول مرة، فلو الـ Bloc جواها بضل يجيب نفس البيانات القديمة. الحل: نعمل الـ Bloc بمستوى أعلى، ونعيد تحميله يدوياً كل ما المستخدم يدوس على تاب Folders.

### 4.5 Settings

- [`settings_page.dart`](lib/features/settings/presentation/pages/settings_page.dart): بطاقة بروفايل (اسم/إيميل/صورة من حساب جوجل أو أبل)، قسم "APPEARANCE" فيه `ThemeModeSelector` (System/Light/Dark)، وزر تسجيل خروج.
- بيستخدم `BlocListener` يستمع لـ `AuthSignedOutState` وينقل المستخدم لصفحة تسجيل الدخول فوراً (بدل ما يعتمد بس على الـ router redirect التلقائي، يلي ما كان يشتغل فوري بالتطبيق).

### 4.6 Shell

- [`main_shell_page.dart`](lib/features/shell/presentation/pages/main_shell_page.dart): هون "الهيكل" يلي بيربط Home + Folders + Settings بشريط تنقل سفلي مخصص ([`app_bottom_nav_bar.dart`](lib/core/widgets/app_bottom_nav_bar.dart))، باستخدام `IndexedStack` (يخلي كل التابات "حية" بالذاكرة، مش يعيد بنائهم كل مرة تبدل تاب — عشان التنقل يكون سريع وسلس بدون ما يعيد تحميل كل شي).

### 4.7 Subscription (الاشتراكات المدفوعة)

**⚠️ هاد الفيتشر كود بس، مش شغال فعلياً حالياً** — لأنه يحتاج حساب Apple Developer ($99/سنة) وGoogle Play Console ($25 مرة وحدة) وحساب RevenueCat، ولا واحد منهم موجود لسا. الكود جاهز لحظة ما تجهّزهم. راجع [قسم "خطوات التفعيل"](#8-شو-لسا-ناقص--خطوات-جاية) بالأسفل.

- **الفكرة:** فولدرز مجانية لحد `SubscriptionConstants.freeFolderLimit` (3 حالياً)، وبعدها لازم اشتراك. شهري **$7**، سنوي **$23** (السعر الحقيقي بينضبط من App Store Connect / Play Console، مش من الكود — الأرقام هون بس نص تحضيري لحد ما تنعمل المنتجات الحقيقية).
- بيستخدم [RevenueCat](https://revenuecat.com) (مكتبة `purchases_flutter`) بدل التعامل المباشر مع StoreKit/Play Billing — بيبسط التحقق من صحة الفاتورة (receipt validation) والتعامل مع الاثنين (iOS + Android) بواجهة وحدة.
- [`revenue_cat_data_source.dart`](lib/features/subscription/data/data_sources/revenue_cat_data_source.dart): كل استدعاء ملفوف بـ `try/catch` بيرجع "مافيش اشتراك" بدل ما يطيح التطبيق — عشان التطبيق يضل شغال 100% حتى قبل ما تنعمل حسابات المتاجر.
- [`folders_page.dart`](lib/features/folders/presentation/pages/folders_page.dart) `_createFolder()`: قبل ما يفتح شيت إنشاء فولدر، بيتحقق من العدد الحالي + حالة الاشتراك؛ لو تجاوزت الحد المجاني وما عندك اشتراك، بيفتح [`paywall_page.dart`](lib/features/subscription/presentation/pages/paywall_page.dart) بدلاً منه.
- الـ `PaywallPage` بترسم بطاقتين (شهري/سنوي) حتى لو المنتجات الحقيقية مش موجودة بعد (بتعرض الأسعار الافتراضية وبتعطّل زر الشراء مع رسالة واضحة "مش متوفر لسا").

### 4.8 Duplicates (كشف السكرين شوتات المكررة)

ميزة مدفوعة، مدخلها من Settings ← STORAGE ← "Find duplicates".

**كيف بيعرف إنو صورتين متشابهين؟** مش بمقارنة الملفات — لأنو نفس اللقطة ممكن تنحفظ مرتين بضغط مختلف فتطلع بايتاتها مختلفة تماماً. بدل هيك مستخدمين **Perceptual Hash** (خوارزمية dHash)، بملف [`perceptual_hash.dart`](lib/core/utils/perceptual_hash.dart):

1. الصورة بتتصغّر لشبكة **9×8** بكسل وبتتحوّل لتدرج رمادي (بمعاملات Rec.601، نفس اللي بتحسب فيها الشاشات السطوع كما تراه العين).
2. كل بكسل بينقارن بجاره على اليمين: أفتح = بت 1، أغمق = بت 0.
3. 8 مقارنات × 8 صفوف = **64 بت** بتوصف "شكل" الصورة (توزيع الفواتح والغوامق) مش محتواها الدقيق.
4. المقارنة بين بصمتين بتصير بـ **Hamming distance** — كم بت مختلف بينهم. متطابقين = 0، متشابهين بصرياً = أقل من 8، مختلفين تماماً = عادةً فوق 25.

**ليش هاي الطريقة؟** لأنها بتشوف الصورة زي ما العين بتشوفها: بما إنها بتخزّن *فروقات* السطوع مش قيمه المطلقة، فهي بطبيعتها ما بتتأثر بإعادة الضغط ولا بتغيّر السطوع العام. وبنفس الوقت خفيفة جداً — بدون أي موديل ذكاء اصطناعي، وبدون أي مكتبة خارجية (استخدمنا `dart:ui` مباشرة لفك الترميز).

**التجميع:** [`duplicates_repository_impl.dart`](lib/features/duplicates/data/repositories_impl/duplicates_repository_impl.dart) بيستخدم **Union-Find**، مش مجرد مقارنة أزواج. السبب: لو A تشبه B وB تشبه C بس A وC مش متشابهين مباشرة، لازم التلاتة يكونوا بمجموعة وحدة — Union-Find بتضمن هالشي.

**اختيار النسخة "الأفضل" للاحتفاظ فيها** (`_keeperRank`) بالترتيب:
1. النسخة يلي المستخدم **نظّمها** (مفضّلة أو محطوطة بفولدر) — حذفها معناه رمي شغله، فهاي أقوى إشارة.
2. الأعلى دقة (بكسلات).
3. الأكبر حجماً بالبايت.
4. الأقدم (النسخة الأصلية).

**الأمان:** التطبيق **ما بيحذف ولا صورة لحاله أبداً**. الفحص بس *بيقترح*، والمستخدم بيقدر يعدّل أي اختيار (يدوس على أي صورة يعكس القرار، أو "Keep all" يلغي المجموعة كلها)، وفي تأكيد صريح قبل الحذف. السبب: خوارزمية التشابه ممكن تغلط، والحذف ما بيرجع.

**الأداء:** البصمات بتتخزّن بـ `sqflite` (عمود `phash`, schema v5)، فالفحص التاني بيحسب بس للصور الجديدة. الكتابة بتصير بـ transaction وحدة لأن الفحص ممكن يلمس مئات الصفوف.

### 4.9 التصنيف التلقائي (Smart Albums) — **محذوف**

الميزة انحذفت من التطبيق كلياً: الكود، الجداول، المدخل من تاب Folders، وحزمة `google_mlkit_image_labeling` كمان.

**ليش؟** لأنها كانت بتفشل بالطريقة الوحيدة اللي ما بتنغفر: بتحط الصورة بألبوم غلط بثقة. جربنا نشدّها (حد أدنى أعلى، رفض الغموض، تجاوز يدوي)، وضلّت التصنيفات "بتحس غلط" للمستخدم. وميزة بتخمّن غلط قدّامك كل يوم بتخسّر ثقتك بكل التطبيق مش بالميزة بس.

اللي بقي منها ومفيد فعلاً:
- **قراءة النص (OCR)** لسا شغالة، بس لهدف واحد صريح: **البحث جوا الصور** (٤.٦) و**استخراج الأفعال** (٤.١١). المستخدم هو اللي بيسأل، فما في تصنيف بيفرض عليه رأي.
- الدرس المعماري: أي ميزة بتصنّف نيابةً عن المستخدم لازم يكون عندها **مخرج "ما بعرف"** واضح، وإذا كانت "ما بعرف" هي الجواب الصح ٤٠٪ من الوقت — الميزة نفسها هي المشكلة.

بديلها المدفوع صار **المشاركة الآمنة (Safe Share)** — شوف ٤.١٢.

### 4.10 دمج السكرين شوتات الطويلة (Scrolling Stitch)

ميزة مدفوعة. بتحدد ٢-٥ سكرين شوتات أخذتها وإنت بتنزل بنفس الصفحة، وShoto بيلاقي مكان التداخل بينهم وبيدمجهم بصورة وحدة طويلة. المدخل: حدد صور من الهوم → زر **Merge** بشريط التحديد.

#### المشكلة أبسط مما تبدو

المستخدم نزل بخط مستقيم، فالصور مختلفة **بإزاحة عمودية فقط**. يعني المسألة أحادية البعد — مش محتاجين تحليل ميزات ولا ذكاء اصطناعي، محتاجين نلاقي **عند أي صف بتتفق الصورتين**.

بس ٣ أشياء بتكسر أي حل ساذج، وكل وحدة متعالجة صراحةً بـ [`scroll_stitcher.dart`](lib/core/utils/scroll_stitcher.dart):

**١) شريط الحالة ما بينزل — وساعته بتتغير كمان.**
مطابقة الصف الأول من الصورة السفلية مع محتوى الصفحة فاشلة حتماً، وبتسمّم كل الاحتمالات. الحل: بنكتشف "الزينة الثابتة" أول — **الصفوف اللي متطابقة بنفس الموضع بالصورتين**. لأن المحتوى تحرّك لما نزلت، فأي صف ما تحرّك هو بالتعريف واجهة ثابتة (شريط حالة، هيدر لاصق، شريط تنقّل). بننحّيها من المطابقة ومن الناتج.

**٢) التكلفة.**
فحص كل إزاحة بالدقة الكاملة = O(الارتفاع²) — مئات الملايين من المقارنات لصورة ٢٤٠٠ بكسل. فبنشتغل على مرحلتين: مرحلة **خشنة** على صفوف متوسَّطة ٨:١ بتحدد المناطق المرشحة، وبعدين مرحلة **دقيقة** بالدقة الكاملة بتستخرج الصف بالظبط.

**٣) الإجابة الغلط أسوأ من لا إجابة.**
محتوى متكرر (قائمة محادثات، جدول) ممكن يعطي تطابق مقنع بمكان غلط تماماً، والنتيجة صورة بتخترع محتوى ما كان موجود. فالنتيجة ما بترجع إلا لما تعدّي **عتبة تشابه مطلقة** *و* تكون متفوقة بوضوح على أقرب منافس مختلف.

#### درسان تقنيان انكشفوا بالاختبار

**أ) المرحلة الخشنة بتحدد الموقع فقط — ممنوع تحكم.**
أول نسخة كانت بتطبّق فحص الثقة على النتائج الخشنة. المشكلة: لما الإزاحة الحقيقية تقع **بالنص بين خطوتين خشنتين**، متوسط الصفوف بيطمس التطابق فتتدهور نتيجته (من ٠ لـ ~٢١) وينرفض المرشح الصحيح. الحل: **إعادة تقييم المرشحين بالدقة الكاملة قبل أي حكم**.

**ب) لازم ننقّح عدة مرشحين مش الأول بس.**
مع محتوى فاتح ومنخفض التباين (مستند أبيض)، التوسيط بيمحي الفروقات لدرجة إن المنطقة الصحيحة **ممكن ما تكون الأولى بالترتيب الخشن**. فبناخد أفضل ٥ مناطق متباعدة، بننقّحهم كلهم، وبعدين بنقرر.

#### مختبرة بمسح شامل

[`test/scroll_stitcher_test.dart`](test/scroll_stitcher_test.dart) — ١٦ اختبار، وأهمهم **مسحان شاملان** بيجرّبوا ~١١٨٦ مسافة تمرير مختلفة (كل بكسل من ١٦ لـ ٦٢٠) بنوعين محتوى.

هاد مقصود: الباگ الأول كان بيضرب عند **إزاحة وحدة محددة** بس. اختبار بإزاحات مختارة يدوياً كان بيمرق. المسح الشامل هو اللي كشفه.

> **ملاحظة دقيقة:** الاختبارات بتتحقق من `advance` (مسافة التمرير) مش `offset`. لما أول صفوف المحتوى تتصادف وتطلع متطابقة بالصورتين، هي **بالتعريف** ما بتنميّز عن زينة ثابتة، فبتنضم لـ `fixedTop` و`offset` بينزاح بنفس المقدار. الاثنين بيضلوا متسقين والناتج بيطلع نفسه بالظبط، لأن هندسة الصورة النهائية بتتحدد بـ `offset - fixedTop`.

#### الأداء والذاكرة

الصور بتنفك مرتين **عن قصد**:
- **مرة أولى**: بنطلب من المحرك نسخة عرضها ٤٨ بكسل بس (بارتفاعها الأصلي) — بضع مئات الكيلوبايت بدل عشرات الميغا، وهي كل اللي بحث الإزاحة بيحتاجه. الارتفاع بيضل كامل لأن الوصلة لازم تقع على صف بالظبط وإلا بتبيّن قصة.
- **مرة ثانية**: بالدقة الكاملة، بس عند التركيب النهائي — ووقتها بس بتكون كل الصور بالذاكرة سوا. عشان هيك الحد ٥ صور.

#### تفاصيل صغيرة بتفرق

- **الترتيب بوقت الالتقاط**، مش بترتيب اختيار المستخدم ولا ترتيب المعرض (اللي بيرجع الأحدث أول).
- **لو فشل الترتيب التصاعدي، بنجرّب المعكوس** — لأن المستخدم ممكن يكون صوّر وهو طالع لفوق.
- **ما بينحفظ شي إلا بعد ما تشوفه**. الكشف التلقائي ممتاز بس مش مؤكد أبداً، وبس عين الإنسان بتقدر تحكم إذا الصفحة بتقرأ صح. المعاينة قابلة للتكبير عشان تفحص الوصلات.
- **لقطتين متطابقتين مش دمج** — هاد تكرار، والميزة بترفضه بدل ما تخترع وصلة.

### 4.11 تحويل الصورة لأفعال (Actionable Screenshots)

ميزة مدفوعة. بتفتح سكرين شوت → زر **Actions** → Shoto بيقرا النص وبيطلعلك اللي ممكن **تعمله**: رقم بتتصل فيه، لينك بتفتحه، كود بتنسخه، آيبان بتحوّل عليه.

**بتشتغل على نص الـ OCR المخزَّن أصلاً** من البحث والألبومات الذكية. ٣ ميزات صارت تتشارك نفس الكاش، والتعرّف ما بينعاد أبداً لنص حدا قراه قبل.

#### المبدأ الحاكم: الدقة أهم من الشمول

رقم هاتف **ضايع** بيكلّف المستخدم نسخة يدوية. رقم هاتف **غلط** بيعرض عليه يتصل بشخص غريب. فكل نمط هون مشدود لحد إنه ما بيطلق إلا على الشي الحقيقي:

| النوع | كيف بنتأكد |
|---|---|
| **آيبان** | تحقق **checksum فعلي** (ISO 7064 mod-97) — مش مجرد شكل |
| **كود تحقق** | لازم كلمة سياق قريبة (`code`, `رمز`, `OTP`) — رقم لحاله مش كود |
| **هاتف** | ٧-١٥ رقم، **مع رفض صريح للتواريخ** (`12/05/2024` كمان ٨ أرقام بفواصل!) |
| **لينك** | نطاقات صريحة، وللنطاقات المجردة **قائمة TLD محددة** — بدونها `2.5.1` بتصير موقع |

#### قاعدة عدم التداخل

الإيميل جواته شي شكله نطاق ويب بالظبط، واللينك مليان أرقام شكلها تلفون. فالأنماط بتتطبق **بترتيب ثابت**، وكل وحدة بتتخطى أي مقطع محجوز من اللي قبلها. نفس النص ما بيقدر يطلّع فعلين.

#### دعم العربية

النص بيمرّ على تطبيع بيحوّل **الأرقام العربية الهندية** (٠١٢٣٤٥٦٧٨٩) والفارسية (۰۱۲۳۴۵۶۷۸۹) لأرقام ASCII. بدونه، أي سكرين شوت من واجهة عربية ما بيطلّع ولا رقم — لأن `\d` ما بتطابقهم إطلاقاً. التحويل حرف بحرف عشان مواقع المطابقات تضل مضبوطة.

#### تفصيلة أمان

زر الاتصال بيستخدم **`tel:` (فتح لوحة الاتصال) مش اتصال مباشر**. يعني لو الـ OCR قرا رقم غلط، مستحيل يتم اتصال حقيقي بدون ما تضغط إنت. وكمان ما بيحتاج أي صلاحية إضافية.

و**النسخ متاح لكل نوع ودايماً آخر خيار** — لأنه الخيار الوحيد اللي ما بيفشل، فبيضل موجود لو الجهاز ما فيه متصفح أو بريد.

#### مختبرة

[`test/action_extractor_test.dart`](test/action_extractor_test.dart) — ٢٢ اختبار، نصّها مخصص **لمقاومة الإيجابيات الكاذبة**: تاريخ ≠ تلفون، رقم طلب ≠ كود، رقم إصدار ≠ لينك، رقم تتبّع ≠ آيبان.

> **باگ انكشف بالاختبار:** أول نسخة كانت بتفشل مع الآيبان المكتوب بمسافات (`GB82 WEST 1234...`) — وهاي **الطريقة اللي البنوك بتطبعه فيها فعلياً**. والحل ما كان مجرد "اسمح بمسافات": المُكمِّم المرن بياكل الكلمات اللي بعد الآيبان بشراهة، وبما إن التحقق بيصير بـ Dart مش بالـ regex، المحرك عمره ما بيرجع للخلف يدوّر على المطابقة الأقصر الصحيحة. فالحل كان **إلزام مجموعات من ٤ خانات بالظبط**.

#### إعداد أندرويد ضروري

أندرويد ١١+ بيخفي التطبيقات التانية إلا إذا انذكرت بـ `<queries>` بالمانيفست. بدونها `canLaunchUrl` بترجع `false` لنوايا شغالة تماماً، وكل الأزرار بتبيّن مكسورة. مضاف: `https`, `tel`, `mailto`.

### 4.12 المشاركة الآمنة (Safe Share) 🛡️

**ما بتغطّي — بتستبدل.** قبل ما تبعت سكرين شوت لحدا، Shoto بيفحصه، وبيرسم مكان كل بيان حساس **بيان تاني عادي بنفس الطول ونفس التنسيق ونفس الموقع ونفس اللون**. النسخة اللي بتبعتها بتبان كأنها ما تعدّلت أبداً — والأصل عندك ما بينلمس.

المدخل: كرت **Safe share** بالهوم، أو أيقونة الدرع بشريط الأدوات بصفحة تفاصيل الصورة.

#### ليش الاستبدال بدل المربع الأسود

المربع الأسود بيشتغل، وبيفضح حاله. أي حدا بيشوف الصورة بيعرف إنه كان في رقم بطاقة، وإنك عارف، وإنك ما بدك ياه يشوفه. نص سبب إن الناس ما بتشارك سكرين شوت أصلاً هو إن النسخة الآمنة **بتبان مسحوبة**.

رقم بطاقة انستبدل برقم بطاقة تاني ما بيعلن ولا إشي.

#### الانقسام المجاني/المدفوع

| المرحلة | مين بيقدر |
|---|---|
| **الفحص** — "لقينا ٤ بيانات حساسة: موقعك، رقم حسابك، اسمك، رقم تلفونك"، مع تأشير مكان كل واحد على الصورة | **الكل، بدون حدود** |
| **التنظيف** — تحويل الكشف لنسخة نظيفة | مشتركين Pro |

الفحص مجاني عن قصد، مش كرم: إنك تقول لحدا إن الصورة اللي رح يبعتها فيها عنوانه ورقم بطاقته إشي بيستاهل يعرفه سواء دفع أو لأ — ولما بتحاسب حدا عشان **يعرف إنه في مشكلة**، بيبطّل يصدّق الفحص. الگيت اتشال من مدخل الشاشة وانحط على زر "نظّف الصورة".

#### شو بيلاقي

[`lib/core/utils/sensitive_data.dart`](lib/core/utils/sensitive_data.dart) — دارت خالص، قابل للاختبار بالكامل:

| النوع | كيف بينكشف |
|---|---|
| بطاقة بنكية | **Luhn checksum** مش مجرد ١٦ رقم |
| IBAN | **ISO 7064 mod-97** |
| رقم هوية | لازم يكون **معنون** ("هوية"، "ID No.") |
| كود تحقق | لازم يكون بسياق ("رمز التحقق"، "OTP") |
| **العنوان / الموقع** | مسمّى ("العنوان:"، "Deliver to")، أو شكل شارع، أو **إحداثيات** |
| **الاسم** | مسمّى بس ("الاسم:"، "Full name"). الاسم بدون تسمية **ما بينتغطّى** — شوف تحت |
| **رقم طلب** | مسمّى + لازم يحتوي رقم |
| إيميل | نمط + إصلاح أخطاء OCR (`ali @ gmail , com`) |
| هاتف | **لازم يثبت إنه هاتف**: مسمّى ("جوال"، "Phone:")، أو `+` كود دولة، أو شكل الموبايل المحلي (بيبدأ بصفر و٩–١١ خانة). ومع **رفض التواريخ** صراحةً |
| **رقم** (بدون اسم) | أي سلسلة ٧–٢٤ خانة ما قدر يسمّيها كاشف تاني |

**ليش الاسم بدون تسمية ما بينتغطّى.** "أحمد خليل" على سطر لحاله هو اسم بس إذا كنت عارف صاحب الصورة. كان في إعداد بالسيتنغ بيسألك اسمك عشان الكاشف يعرفه، وانشال — الصف كان مضايق، والسؤال عن معلومة شخصية على تطبيق كل وعده إنه ما بيجمع إشي هو سؤال تقيل. الكواشف الباقية كلها بتشتغل بدون ما تعرف عنك إشي: البطاقة عندها checksum، والـIBAN عنده mod-97، والباقي عندهم تسمية. المطابقة نفسها لسا موجودة ومختبَرة بـ`SensitiveData.findIn(ownerNames:)`، بس ما ضل حدا بيمرّرلها اسم.

**ليش في نوع اسمه "رقم".** قبل هيك، كل سلسلة من ٧ لـ١٥ خانة بدون تسمية كانت بتنسرد للمستخدم كـ"رقم تلفون" — فرصيد حساب، ورقم غرفة، وقراءة عدّاد، ورقم بوليصة، كلهم كانوا "رقم تلفون". قائمة مراجعة تسمياتها غلط ما حدا بيقراها. **التغطية ما كانت المشكلة، الادعاء كان المشكلة** — النوعين بينتغطّوا نفس الشي بالظبط، فالفصل ما كلّف ولا إشي بالأمان وشال ادعاء التطبيق ما إله أساس. والسقف طلع لـ٢٤ خانة كمان: رقم حساب من ١٩ خانة بيفشل بـLuhn كان بيطلع من كل الكواشف ويضل ظاهر بالنسخة المشاركة.

**ولا كشف واحد بيخمّن اسم.** كلمتين بحرف كبير بتكون اسم شخص بالشات واسم زر بكل مكان تاني، وتطبيق بيحوّل "Order Details" لـ"James Miller" أسوأ من تطبيق بيفوّت اسم — **التغطية الغلط بتبيّن، الاستبدال الغلط لأ.** فالاسم بينكشف بحالتين بس: الصورة نفسها بتقول إنه اسم، أو التطبيق أصلاً بيعرف الاسم لأنك سجّلت دخول فيه. الاتنين حقائق مش استنتاجات.

#### ترتيب الكشف = قواعد اتعلّمناها بالغلط

الكواشف بتشتغل بترتيب ثابت وكل واحد بيتخطّى اللي حجزه اللي قبله:

1. **الشكل المميّز بيغلب المسمّى.** الإحداثيات بتنحجز أول إشي، وإلا كاشف التلفون بياكل `24.774265` كرقم من ٨ خانات.
2. **IBAN قبل البطاقة.** IBAN من ٢٤ خانة جواته سلاسل من ١٨ رقم، وواحدة من كل عشرة بتعدّي Luhn بالصدفة — فكاشف البطاقة كان بيسمّي حسابات بنكية "بطاقة" بهدوء. اكتشفها اختبار المولّد لما ولّد IBAN صحيح والكاشف قراه بطاقة.
3. **المسمّى بيغلب الشكل.** `Order number 4567890` سبع خانات، وهاد كمان شكل رقم تلفون — بس الصورة قالت شو هو، والتسمية دليل أقوى من الطول. فكل الكواشف المعنونة بتسبق كاشف التلفون الحر.
4. **العناوين بالآخر مع قص.** العنوان سطر طويل بيحتوي رقم تلفون عادةً، فبينحجز آخر إشي و**بينقص** حوالين اللي محجوز — عشان التلفون يضل مسرود كتلفون بدل ما يختفي جوّه "عنوان".

**العربي:** الأرقام العربية-الهندية (`٠١٢`) والفارسية (`۰۱۲`) بتنطبّع للاتيني قبل أي فحص. والتطبيع بيحافظ على الطول حرف بحرف عشان كل موقع بينلاقى بالنسخة المطبّعة يكون موقع صحيح بنص المستخدم — وأي تطبيع تاني (توحيد الهمزة، شيل الحركات، طي الحالة) بيحمل **خريطة فهارس** للأصل، لأن span انلاقى بإحداثيات نسخة غير الأصل بينرسم على كلمات تانية.

#### من "أي أنواع موجودة" لـ"وين بالظبط"

الكاشف كان بيرجّع `Set<SensitiveKind>` — كافي تماماً لمربع أسود فوق السطر كله. الاستبدال بده الفهارس: `Card number: 4111 1111 1111 1111` لازم يضل عنده كلمة "Card number"، وتتغيّر الأرقام بس. فصار يرجّع `List<SensitiveMatch{kind, start, end, value}>`.

وعشان نحوّل مدى حروف لمستطيل على الصورة، `recognizeLines()` صار يجيب **الكلمات وصناديقها** كمان (`TextLine.elements`). [`text_line_geometry.dart`](lib/core/utils/text_line_geometry.dart) بيلاقي موقع كل كلمة جوّه نص السطر بمؤشّر بيمشي للأمام بس (سطر فيه `1 1 1` عنده ٣ كلمات متطابقة — البحث من الصفر كل مرة بيحطّهم كلهم على الأولى)، وبيرجّع اتحاد صناديق الكلمات اللي بتتقاطع مع المدى.

**الاتحاد لازم يغطّي القيمة كاملة وإلا فهو أسوأ من لا إشي.** `_locate` بتتخطّى أي كلمة ما بتلاقيها حرفياً جوّه نص السطر — وهاد بيصير أكتر مما بيبان: المحرّك حر يرجّع الكلمة بمسافة مختلفة أو حرف مقروء غلط عن اللي كتبه بالسطر. لما الكلمة المتخطّاة هي اللي فيها آخر أربع خانات، كل الحساب فوق بينجح وبيرجّع مستطيل — مستطيل بيغطّي **معظم** رقم البطاقة. هاد بالظبط شكل العطل اللي المستخدم بيوصفه بـ"غطّى كل إشي إلا وحدة"، فصار ينفحص مش ينفترض: إذا أي جزء مرئي من المدى وقع برّا الكلمات المعروفة، السطر كله بينرسم. أوسع من اللازم، وأبداً مش غلط.

وبنفس المنطق، `RedactionService` **ما بيرمي أي كشف** لأن مستطيله ما انحسب. الكشف اللي بيوصل لهون انعرف إنه حساس؛ رميه بصمت هو العطل الوحيد اللي هاي الميزة ممنوع يصير فيها — القائمة ما بتذكره، البريفيو بيبين نظيف، والقيمة بتنبعت بالملف. إذا الهندسة فشلت، بينغطّى السطر اللي إجا منه.

#### السطرين اللي بينقروا كسطر واحد

الحقل بالفورم بينقسم على سطرين كثير: التسمية بسطر والقيمة تحته. فحص كل سطر لحاله عمره ما بيشوف هاد — "رقم الهوية" لحاله ما فيه أرقام، و"1234567890" لحاله عشر خانات بتكون أي إشي، فالكاشف اللي بده تسمية بيرفض النصين. **الكاشف مضبوط؛ الوحدة اللي وصلته كانت صغيرة.**

[`line_stitching.dart`](lib/core/utils/line_stitching.dart) بيقرر **أي سطرين مسموح ينقروا كسلسلة واحدة** وبس — ما بيقرر شو حساس، فبينتفحص بدون صورة ولا محرّك ولا جهاز. والشرط صارم عن قصد: نفس البلوك اللي المحرّك جمعه، وواحد تحت التاني (مش عمود جنب عمود)، ومتقاطعين أفقياً، والفراغ بينهم أقل من ارتفاع سطر. الفاصل **مسافة مش سطر جديد** — وهاي كل الآلية: كل أنماط التسمية بتفصل بمسافة أفقية بس، بالظبط عشان تسمية بآخر سطر ما توصل للسطر اللي بعده؛ هون الوصول هو المطلوب، بس محصور بسطرين الهندسة ضمنتهم.

الفحص صار مرحلتين: مرحلة لكل سطر لحاله (هون بينلاقى شبه كل إشي)، وبعدها مرحلة على الأزواج. **`SensitiveKind.number` مرفوض من المرحلة التانية** — هو بيعرف شكل وبس، وعبر وصلة بيقرا آخر سعر بسطر مع أول كمية بالسطر اللي بعده كرقم واحد طويل ويسوّد الاتنين. وبالعكس: كشف مسمّى من المرحلة التانية **بياخد محل** كشف "رقم" من المرحلة الأولى على نفس الحروف — نفس البكسلات، بس التطبيق صار بيعرف إنها رقم هوية.

**ما في تخمين لمواقع الحروف جوّه الكلمة.** لو المدى وقع بنص كلمة، المدى بيكبر للكلمة كاملة والمتصل بيعيد بناء نصها بالجزء الحساس مستبدَل بس. التخمين بيشتغل باللاتيني وبينعكس بهدوء بالعربي، واستبدال بمحل كلمة عاليسار أسوأ من واحد بيغطّي كلمة زيادة.

#### البديل لازم يعدّي نفس الفحص

[`fake_identity.dart`](lib/core/utils/fake_identity.dart) — نفس الشكل، نفس الأرقام، صالح فين ما الصلاحية بتبيّن:

- **البطاقة**: نفس الطول ونفس التجميع، أول خانة محفوظة (نوع البطاقة، وأصلاً شعارها مرسوم جنبها بالصورة)، والخانة الأخيرة محسوبة عشان تعدّي **Luhn**.
- **IBAN**: نفس الدولة ونفس الطول، وخانات التحقق محسوبة عشان تعدّي **mod-97**.
- **التلفون**: `+` ومفتاح الدولة محفوظين، والباقي عشوائي — رقم سعودي فجأة بيبدأ `+1` بيفضح حاله زي المربع الأسود.
- **الأرقام العربية**: لو الأصل مكتوب `٤١١١`، البديل بينكتب `٤١١١`. هاد أكتر تفصيل بيفضح التعديل.
- **شخص واحد بالصورة**: بيرسونا وحدة لكل صورة، فالاسم فوق والإيميل بالنص والعنوان تحت كلهم لنفس الشخص المخترع. تلات هويات وهمية مش مترابطة بفاتورة وحدة هي نوع تاني من الفضيحة.
- **حتمي**: نفس الصورة بتعطي نفس البديل دايماً (xorshift مكتوب بالإيد، مش `Random` اللي تسلسله تفصيل داخلي بالـSDK). فحص مرتين وطلوع اسمين مختلفين بيخلي الميزة تبيّن كأنها بتخمّن.

**الاختبار الأقوى: البديل لازم يخدع كاشف Shoto نفسه.** ٢٠٠ بذرة لكل من البطاقة والـIBAN، وكلهم لازم ينكشفوا كبطاقة/IBAN صحيحة.

#### الألوان بتنقرا من الصورة نفسها

[`text_appearance.dart`](lib/core/utils/text_appearance.dart) — دارت خالص فوق مصفوفة RGBA:

- **لون الخلفية** = أكتر لون متكرر بالمنطقة + هامش حواليها. الكوانتايز على ٥ بت لكل قناة، لأن الـanti-aliasing بيخلي "لون واحد" فعلياً مئات الألوان المتقاربة والعد الدقيق بيفرّق الخلفية عليهم كلهم.
- **لون الحبر** = أبعد لون عن الخلفية بيحتل حصة كافية عشان يكون حرف مش نقطة.
- **`uniformity`** = قدّيش السطح مسطّح. **هاد اللي بيقرر إذا نستبدل أصلاً**: شات أو كشف حساب سطح لون واحد والرقعة بتختفي فيه؛ صورة أو تدرّج لأ، وهناك المربع الصادق أحسن من الرقعة البايظة. تحت ٠.٥٥ المنطقة بتبلّش على **تغطية** مش استبدال.
- **`inkShare`** = قدّيش الحروف تقيلة، عشان عنوان bold ما ينرسم regular.

#### التحكم: ٣ حالات لكل بند

كل بند بالقائمة إله **استبدال / تغطية / اتركه** ظاهرين مباشرة — مش سويتش بإعداد مخفي. الشاشة كلها موجودة عشان ما حدا يبعت صورة وهو مفكّر إن إشي انعالج وهو ما انعالج، وكونترول لازم تفتحه عشان تقراه بيشتغل ضد هاد.

#### باگين صامتين — وهاد ليش إلهم اختبارات لحالهم

بالرسم، بندين بنفس المقطع بيكونوا **سطرين بالقائمة وقطعة وحدة بالصورة**. رسمها مرتين معناه إن التمريرة التانية بتعيد بناء النص من **الأصل** — فبترجّع القيمة الحقيقية اللي التمريرة الأولى لسه مستبدلاها. والاستبدالات بتتطبّق **من الآخر للأول**، لأن البديل نادراً بنفس طول الأصل والتطبيق من الأول بيخرّب كل مدى بعد الأول.

الاتنين بيفشلوا **بصمت**: المعاينة بتقول انستبدل، الملف المصدَّر لسه فيه الرقم، وما حدا بيعرف إلا بعد ما تنبعت الصورة. مغطّيين بـ[`test/redaction_composition_test.dart`](test/redaction_composition_test.dart).

#### الخط: **ما بينتسمّى ولا خط**

المتصل ما بيمرّر `fontFamily` إطلاقاً، فالمحرك بياخد خط النظام — وهاد بالظبط الخط اللي معظم التطبيقات اللي بالصورة كانت بترسم فيه. لو مرّرنا خط Shoto، بنكون حطّينا خط تطبيقنا جوّه واجهة حدا تاني، وكلمة وحدة بخط غلط هي التفصيل اللي بيفضح التعديل.

#### المعاينة = الملف المصدَّر

المعاينة بعد الفتح مش رسم بالـwidgets، هي **بايتات الـPNG نفسها** اللي رح تنبعت. تغيير أي حالة بيعيد الرسم بعد ٢٢٠ms (debounce) مع `gaplessPlayback` عشان ما ترفّ. أغلى من رسم تقريبي، بس اللي بتفحصه هو اللي بيوصل.

وقبل الفتح، المعاينة بتعرض الأصل مع **ضبابية فوق كل بند** — بتجاوب "وين؟" اللي هو كل وعد الفحص المجاني، بدون ما تجاوب "شو بيصير لو انظّفت؟" اللي هو اللي عم ينباع. طبقة blur وحدة مقصوصة على كل المناطق مع بعض، مش وحدة لكل منطقة: فاتورة ممكن تحمل عشرة، وعشر `BackdropFilter` فوق بعض عشر قراءات كاملة للسطح بكل فريم.

#### مختبرة

| الملف | شو بيثبت |
|---|---|
| [`sensitive_data_test.dart`](test/sensitive_data_test.dart) | إن الأنواع بتنكشف، وأهم إشي إنها **ما بتنكشف** غلط: تاريخ مش تلفون، رقم طلب مش بطاقة |
| [`sensitive_span_test.dart`](test/sensitive_span_test.dart) | إن الفهارس والنص متفقين — off-by-one هون معناه خانة من رقم بطاقة حقيقي باينة على حرف الرقم المزيف |
| [`fake_identity_test.dart`](test/fake_identity_test.dart) | إن البديل بيخدع كاشف Shoto نفسه، ٢٠٠ بذرة لكل نوع |
| [`text_appearance_test.dart`](test/text_appearance_test.dart) | قراءة الألوان، ورفض الاستبدال على صورة أو تدرّج |
| [`redaction_composition_test.dart`](test/redaction_composition_test.dart) | الباگين الصامتين فوق |
| [`redaction_render_test.dart`](test/redaction_render_test.dart) | **الرسم الحقيقي، مفحوص بالبكسل**: البكسلات الأصلية راحت، وولا إشي برّا المنطقة انلمس |

---

### 4.13 الحفظ بضغطة وحدة من شيت المشاركة ⚡

تعمل سكرين شوت → Share → Shoto → **بيطلع بوتوم شيت من تحت فوق التطبيق اللي إنت فيه**، بتختار فولدر وبتضغط Save، وبيسكّر. **ما بتنتقل لـShoto إطلاقاً.**

#### كيف انعملت

الجزء الصعب مش Flutter، Android:

| القطعة | ليش |
|---|---|
| `ShareActivity` منفصلة عن `MainActivity` | عشان ما تدخل على تاسك التطبيق ولا تطلع بالـrecents (`taskAffinity=""` + `excludeFromRecents`) |
| `getBackgroundMode() = transparent` | نافذة Flutter شفافة، فالتطبيق اللي ورا بيضل باين |
| `ShareTheme` بـ`windowIsTranslucent` + `windowAnimationStyle=@null` | بدون هدول الشاشة بتصير بيضا للحظة، والانيميشن تبع النظام بيتعارك مع انيميشن الشيت |
| `getInitialRoute() = "/share"` | Flutter بيبدأ على مسار مختلف |
| `MethodChannel "shoto/share"` | `getSharedImages` بيرجّع `{images: [{path, mediaId}], skipped}`، و`close` بينادي `finishAndRemoveTask()` |

وبـ[`main.dart`](lib/main.dart) بنتفرّع **قبل** `runApp`:

```dart
final bool isShareSheet =
    PlatformDispatcher.instance.defaultRouteName == '/share';
runApp(isShareSheet ? QuickSaveApp() : MyApp());
```

التفرّع هون مش جوا الراوتر عن قصد: الشيت **مش صفحة من صفحات Shoto**، هو شاشة قائمة بحالها.

#### الحركة

الشيت بينزلق بـ`AnimationController` واحد بيسوق **عتامة الحجاب** و`FractionalTranslation` مع بعض، فالاتنين ما بيختلفوا ولا فريم. بينسحب لتحت بالإصبع (`velocity.dy > 700 || _dragOffset > 130`)، و`_close()` **بيرجّع الانيميشن للخلف قبل** ما ينادي القناة — عشان ما تختفي النافذة فجأة.

والانتقال بين مراحل الشيت (تحميل → جاهز → تسمية فولدر → حفظ → تم) بـ`AnimatedSize` + `AnimatedSwitcher`، فالشيت بيكبر وبيصغر بنعومة بدل ما ينطّ.

#### إنشاء فولدر **وإنت جوا الشيت**

لو المستخدم لسا ما عمل ولا فولدر، الشيت ما بيعطيه قائمة فاضية — بيعطيه كرت **"Create your first folder"** بيفتح نموذج تسمية **جوا نفس الشيت** (اسم + لون)، وبعد الإنشاء بيرجع لمرحلة الحفظ **والفولدر الجديد محدّد أصلاً** (سؤاله يختاره مرة تانية بعد ما سمّاه = نفس السؤال مرتين).

ولو عنده فولدرات، آخر شريحة بشريط الفولدرات هي شريحة **"New"** لنفس الغرض.

هاد كلّه محكوم بقاعدة وحدة: **الشيت ما بياخدك لحدا**. لو خلّيناه يفتح التطبيق عشان يعمل فولدر، بنكون كسّرنا الوعد الوحيد اللي بيقدمه.

وشريحة **"New" صارت أول الشريط مش آخره**: الفولدر اللي بدك ياه كتير بيكون هو اللي لسا مش موجود، وبآخر شريط أفقي بيتزحلق كانت مخفية عملياً ورا كل الفولدرات اللي عندك.

#### ما في خيار "احفظ بدون فولدر"

كان في شريحة "Library" بتحفظ الصورة بالمكتبة وخلص. انحذفت **بقصد**: الحفظ بدون تصنيف بيعمل كومة تانية لازم ترتّبها بعدين — وهاي بالضبط المشكلة اللي Shoto موجود عشانها. فالزرار بيضل معطّل لحد ما تختار فولدر، وبيكتب `Pick a folder` بدل ما يدّعي إنه بيعمل إشي.

#### مشاركة أكتر من صورة مرة وحدة

أندرويد بيمثّل هاد **بـaction تاني بالكامل**، مش بـaction واحد بقائمة فيها عنصر:

| | الـaction | شكل `EXTRA_STREAM` |
|---|---|---|
| صورة وحدة | `ACTION_SEND` | `Uri` |
| كذا صورة | `ACTION_SEND_MULTIPLE` | `ArrayList<Uri>` |

يعني تطبيق معرّف `SEND` بس **ما بيظهر أصلاً** بشيت المشاركة لما تختار أكتر من صورة. الحل تلات قطع لازم يكونوا مع بعض:

1. **الـmanifest**: الـ`<intent-filter>` تبع `ShareActivity` صار فيه الـactionين.
2. **Kotlin**: `readImageUris()` بيقرأ الشكل الصح حسب الـaction — قراءة الشكل الغلط بترجّع `null`، مش خطأ.
3. **Dart**: الشيت صار يشتغل على `List<_SharedImage>`، وبيعرض معاينة مكدّسة (٣ صور + `+N`).

**سقف ٣٠ صورة** (`MAX_IMAGES` بـ`ShareActivity`): كل صورة داخلة بتتنسخ لكاش التطبيق **قبل** ما الشيت يعرض إشي، فاختيار مفتوح معناه المستخدم بيبصّ على شاشة فاضية بينما بتتكتب مئات الملفات. واللي فوق السقف **بينقال** (`skipped`) مش بينحذف بالسكوت.

وصورة وحدة بتفشل بالنسخ بتنتخطّى بدل ما تفشّل المشاركة كلها — تختار ١٠ سكرين شوت وما توصل ولا وحدة لأن وحدة منهم مش مقروءة، صفقة خاسرة.

**تفصيلة كيبورد:** الـ`Scaffold` معطّى `resizeToAvoidBottomInset: false`، والشيت بيرفع حالو بـ`MediaQuery.viewInsetsOf(context).bottom`. لو تركنا Scaffold يتصرّف، بيقلّص **الحجاب** كمان فبيبين شريط من شاشة الموبايل تحت — والنافذة شفافة، يعني اللي بيبين هو شاشة المستخدم الرئيسية.

**تفصيلة ثيم:** `QuickSaveApp` بيقرأ `ThemeController` نفسه اللي بيقرأه التطبيق، **مش** `platformBrightness`. قبل هيك كان الشيت بيطلع غامق فوق تطبيق فاتح لما يختلف إعداد النظام عن إعداد Shoto — وبيحس كأنه واجهة تطبيق تاني.

---

### 4.15 البحث البصري — لما ما يكون في نص أصلاً 🐈

البحث كان بيقرأ الـOCR بس: الكلام **المطبوع جوا** الصورة. وهاي فيها ثغرة ما بتنسدّ أبداً بتحسين القراءة — **صورة قطة ما فيها ولا كلمة**. فمهما صار الـOCR أذكى، ما بيوصلها.

فصار في مصدر تاني: موديل رؤية بيشتغل **على الجهاز** ([`image_labeling_data_source.dart`](lib/features/screenshots/data/data_sources/image_labeling_data_source.dart)) بيسمّي اللي جوا الصورة — `Cat`، `Food`، `Ice cream`.

#### المشكلة الحقيقية مش الكشف، الترجمة

الموديل بيطلّع **إنجليزي بس**، وبيطلّع `Cat` — **ما بيقول "حيوان" ولا مرة**. وإنت بتكتب "قطة" أو "حيوان". [`visual_vocabulary.dart`](lib/core/utils/visual_vocabulary.dart) هو الجسر، وقايم على تلات أفكار:

1. **الـlabel نفسه دايماً قابل للبحث بكلمته الإنجليزية.** كل اللي تحت **إضافي**. لو غلطنا باسم label، أو الموديل تحدّث وطلّع اسم ما توقّعناه، بتضل تلاقي الصورة بـ`quokka`. **الثغرة بتكلّف دعم العربي لـlabel واحد، مش الميزة.** وهاد كمان بيخلّي الإفراط بالإضافة **مجاني** — label ما بيطلّعه الموديل أبداً ببساطة ما بيطابق إشي، فالجداول بتقدر تكون كريمة.
2. **المرادفات بتحمل العربي**: `Cat` → قطة، قطط، بسة، هرة، kitten.
3. **المجموعات بتحمل السؤال العام**: "حيوان" مربوطة بـ٤٠+ label، لأن ما في label واحد بيجاوب عليها.

#### المطابقة بالبادئة من الاتجاهين، مش `contains`

```dart
term.startsWith(needle) || needle.startsWith(term)
```

"قط" بتلاقي "قطة" (لسا بتكتب)، و"cats" بتلاقي `Cat` (جمع مش مكتوب بالجدول). بس **"at" ما بترجّع كل القطط** — وهاد اللي كان بيصير مع `contains`. بحث بيجاوب على أسئلة ما حدا سألها أسوأ من بحث بيفوّت.

#### التطبيع العربي إجباري

أ/إ/آ/ٱ→ا، ى→ي، **ة→ه**، وحذف الحركات والتطويل — بينطبّق على **الاستعلام وعلى كل كلمة مخزّنة**، فما في جدول لازم ينكتب بحذر. "قطه" و"قطة" نفس الكلمة وما حدا منهم غلطان. (المصنّف القديم شحن bug مرة لأن ة→ه كانت ناقصة — كل ميزة عربية بهاد التطبيق احتاجت هالخطوة.)

#### عتبة ثقة 0.6 مش 0.5

هدول labels بتغذّي **فهرس بحث**. الـlabel الغلط ما بيقعد ساكت بمكانه — بيخلّي صورة تطلع تحت كلمة ما إلها علاقة فيها، والنتائج اللي ما بتنفسّر هي اللي بتخلّي الناس تبطّل تثق بصندوق البحث.

#### الصورة بتقول **ليش** طلعت

شارة صغيرة عليها اسم الـlabel فوق المصغّرة. مطابقة النص ما بدها تفسير — الكلمة مكتوبة بالصورة. بس المطابقة البصرية **رأي الموديل**، ولازم تشوفه عشان تقدر تخالفه.

**وهاد بالضبط الفرق عن Smart Albums اللي انحذف (٤.٩):** تخمين غلط بنظام أرشفة = ضرر خفي، بتكتشفه لما تلاقي أشياءك بأماكن غلط. تخمين غلط بصندوق بحث = بحث ما لقى، بتشوفه بنص ثانية وبتغيّر الكلمة. **نفس الموديل، نفس الدقة — والفرق كله بمين بيتحقّق من النتيجة.**

#### التخزين

عمود `screenshot_meta.visual_labels` — **اللي كان ميت** من أيام Smart Albums، فما احتجنا ترقية. JSON مش نص مفصول بفاصل، لأن الـlabels فيها مسافات (`Ice cream`, `Interior design`) وأي فاصل بنختاره رح يظهر جوا label يوم من الأيام ويقسمه نصين — و label مقسوم نصين ما بيطابق إشي. وقائمة فاضية بتتخزّن كـ`[]` مش `NULL`: "بصّينا وما لقينا" لازم تنفرق عن "ما بصّينا".

٢٤ اختبار: [`visual_vocabulary_test.dart`](test/visual_vocabulary_test.dart) و[`visual_label_codec_test.dart`](test/visual_label_codec_test.dart).

---

### 4.14 المكتبة **اختيارية** — التطبيق ما بيقرأ معرضك

هاد أهم قرار منتج بالتطبيق كلّه، وانتخذ متأخر.

**قبل:** Shoto كان بيقرأ ألبوم Screenshots تبع الجهاز وبيعرض كل سكرين شوت موجود. النتيجة إن المكتبة صارت **مرآة للمعرض** — نفس الصور، بواجهة تانية. والمستخدم سأل بالضبط: "شو الفايدة؟"

**بعد:** الصورة بتدخل SHOTO **بس** لما المستخدم يشاركها عن قصد. وSHOTO بيقرأ **ألبوم واحد**، ألبومه هو (`Pictures/SHOTO`).

```dart
Future<AssetPathEntity?> _ourAlbum() async { ... paths.where((p) => p.name == importAlbumName) ... }
```

في [`screenshot_gallery_data_source.dart`](lib/features/screenshots/data/data_sources/screenshot_gallery_data_source.dart) انحذف: البحث عن ألبوم "screenshot"، و**الفحص الاحتياطي لآخر ٢٠٠٠ صورة** بالمعرض. ما في أي مسار بيوسّع البحث — التوسيع هو بالظبط السلوك اللي بنتجنبه.

وحتى `findLibraryAsset()` (اللي بيمنع التكرار وقت المشاركة) صار **بس** بيعتبر الصورة "موجودة عندي" إذا كانت بألبومنا. سكرين شوت قاعد بالمعرض **مش** جوا Shoto — وهاد جوهر الفكرة.

**ليش هاد بيقوّي التطبيق مش بيضعّفه:**
1. المكتبة صارت **منتقاة**. اللي جوا Shoto هو اللي إلو قيمة فعلاً.
2. الرقم على الهوم صار معناه شي: "٧ بدها ترتيب" مش "٤٠٠ صورة معرض".
3. الوعد للمستخدم صار صريح وقابل للتحقق: **التطبيق ما بيشوف صور ما أعطيتوها إياه.**

**الإذن لسا مطلوب** — بس لسبب مختلف تماماً: عشان Shoto يقدر **يكتب** بألبومه ويقرأ منه. ورسالة الإذن بتقول هاد بالحرف بدل ما تقول "عشان يلاقي سكرين شوتاتك".

**التكرار:** المشاركة بتنسخ الصورة لألبوم Shoto، فبيصير عندك نسختين — الأصل بالمعرض والنسخة بShoto. هاد **مقصود**: حذف صورة من Shoto لازم ما يحذفها من معرضك، والعكس. ولو شاركت نفس الصورة مرتين، الشيت بيعرف إنها موجودة (عن طريق mediaId) وبيعرض **"File it"** بدل "Save now" — بينقلها لفولدر بدل ما ينسخها من جديد.

## 4.5b تعدد اللغات (٦ لغات) 🌍

التطبيق بيدعم: **الإنجليزية، العربية، الأوردو، الإسبانية، الفرنسية، الهندية** — والافتراضي إنه بيتبع لغة الجهاز.

### البنية

`flutter_localizations` + ملفات `.arb` تحت [`lib/l10n/`](lib/l10n/)، و`generate: true` بالـpubspec فالكود بينولّد مع كل بناء. الوصول عبر `context.l10n.someKey` (اختصار بـ[`l10n.dart`](lib/core/localization/l10n.dart)) — الشكل الطويل `AppLocalizations.of(context)!` بيتكرر ٤٠٠ مرة بالتطبيق، وعلى هالكثافة الفرق بين اختصار وطويل هو الفرق بين إن المطوّر يستخدم النصوص المترجمة أو يتجنّبها.

### `AppLanguage` — اللغة أكتر من جدول نصوص

[`app_locales.dart`](lib/core/localization/app_locales.dart) بيربط بكل لغة الأشياء اللي بتلزمها فعلاً:

| | العربية | الأوردو | الهندية | باقي |
|---|---|---|---|---|
| الاتجاه | RTL | RTL | LTR | LTR |
| الخط الاحتياطي | Noto Sans Arabic | Noto Nastaliq Urdu | Noto Sans Devanagari | — |
| مضاعِف ارتفاع السطر | ١ | **١.٤٥** | ١ | ١ |

حطّينا هالحقائق **جنب اللغة** مش موزّعة على فحوصات `if (locale == 'ar')` بكل الكود — عشان إضافة اللغة السابعة ما تتحوّل لمراجعة كاملة للمشروع.

**ليش الأوردو بدها سطور أطول؟** لأنها بتنكتب بخط **النستعليق**، وهو خط بينزل بشكل مائل داخل الكلمة الواحدة. على ارتفاع سطر لاتيني، نزول سطر بيصطدم بالسطر اللي تحته.

**ليش ما في خط صيني؟** كان مطلوب بالبداية وانستبدل بالأوردو، بس السبب التقني بيضل مهم: `Noto Sans SC` حجمه **١٧.٧ ميجا** — أكبر من التطبيق كلّه — وكل جهاز أندرويد وiOS بيجي بخط CJK ممتاز فلاتر بيرجعله لحاله.

### قرار مهم: قائمة الخطوط الاحتياطية **كاملة** على كل نمط نص

مش خط اللغة النشطة بس. لأن لغة التطبيق ما بتقول ولا إشي عن شو معروض على الشاشة: أسماء المجلدات، أسماء القواعد، والنص المقروء من اللقطات — كلها من عند المستخدم بأي لغة كتبها. تطبيق مضبوط على الفرنسية لازم يعرض اسم مجلد عربي.

### RTL

- `MaterialApp` بيضبط `Directionality` لحاله من اللغة.
- كل حشوة أفقية **غير متناظرة** (يعني يسارها ≠ يمينها) انحوّلت لـ`EdgeInsetsDirectional.fromSTEB` — لأنها مصمّمة تلاصق أيقونة بالبداية أو زر بالنهاية، وهدول بينعكسوا بالعربي. ١٥ موضع + موضعين `EdgeInsets.only(left:)`.
- الحشوات المتناظرة انتركت زي ما هي عن قصد: `symmetric(horizontal:)` أصلاً بينعكس لحاله بلا معنى.

**عيب حقيقي انكشف أثناء الاختبار:** نص إنجليزي غير مترجم داخل واجهة RTL بتطير نقطة نهايته لأول السطر (`.the last` بدل `the last.`). فأي شاشة ما تترجمت بتبيّن مكسورة مش بس بلغة تانية — وهاد اللي خلّى إكمال الترجمة مسألة عرض مش رفاهية.

### منتقي اللغة

بوتوم شيت مش أزرار مقسّمة زي الثيم — ٦ خيارات بأسماء طويلة وستة خطوط ما بتزبط بصف واحد. وكل لغة معروضة **باسمها بلغتها** (العربية، اردو، हिन्दी): قائمة لغات مكتوبة بلغة ما بتقرأها عديمة الفائدة تماماً للشخص اللي محتاجها.

---

## 5. نظام التصميم (Design System)

الهوية الحالية اسمها **"Charcoal"**: كل الرماديات **محايدة تماماً** (الأحمر = الأخضر = الأزرق بالظبط) + لهجة بدون لون — فحمي `#262626` بالوضع الفاتح، وعظمي `#EBEBEB` بالغامق.

**ليش فحمي مش رمادي متوسط؟** مش مسألة ذوق: الرمادي المتوسط هو لغة **الزر المعطّل** بكل المنصات. لو عبّيت زر شغّال بـ `#4A4642` بيقرا كأنه مطفي مهما كانت الكلمة صح. الأسود-القريب بيقرا كحبر: مقصود، فعّال. وبالوضع الفاتح الفحمي أفتح شوي من لون النص عمداً، فالنص بيضل أغمق إشي بالصفحة وما بيغلب عليه زر.

**وبالوضع الغامق بينقلب لعظمي، مش بيفتح شوي** — الفحمي على خلفية شبه سودا ما بينشاف. و`onPrimary` بينقلب معه، فالنص اللي فوق اللهجة بيضل مقروء بالوضعين. (القاعدة نفسها اللي كسرها الأصفر قبل هيك وانشال بسببها: لهجة ما بتقدر تستخدمها بالوضعين مش لهجة.)

بقيت الألوان — تركوازي (نقل)، زيتي (تمّ)، أحمر (حذف) — ضلّت لأن كل وحدة **بتعني إشي**. مع لهجة بلا لون، صاروا الألوان الوحيدة بالتطبيق، وهاد المقصود.

- [`app_colors.dart`](lib/core/theme/app_colors.dart): كل الألوان بمكان واحد. الألوان الأساسية ثابتة بالوضعين، والمحايدات بقيم مختلفة حسب الوضع.
- [`app_text_styles.dart`](lib/core/theme/app_text_styles.dart): ثلاث خطوط بثلاث وظائف — **Archivo** للعناوين، **IBM Plex Sans** للنص، **IBM Plex Mono** للأرقام والبيانات.
- [`app_theme.dart`](lib/core/theme/app_theme.dart): بيبني `ThemeData` **مرتين** (فاتح وغامق).
- [`app_motion.dart`](lib/core/theme/app_motion.dart): مُدد ومنحنيات الحركة + `PressableScale`.
- ويدجيتس مخصصة: `PrimaryButton`, `ShotoLogo`, `AppBottomNavBar`, `EmptyState`, `GoogleLogo`, `ThemeModeSelector`, `PressableScale`.

### 5.0 قرارات التصميم الجوهرية

**المحايدات بدون أي ميلان لوني.** البالتة جرّبت العائلتين: رمادي بارد (أسود مزرق — الشكل الافتراضي لكل واجهة غامقة مولّدة) ورمادي دافئ (الأحمر أعلى قناة بنقطتين تلاتة، بهدف يقرا كورق وقلم رصاص). الدافئ كان فكرة أحسن وبرضو غلط **بهالمقاس**: تلات نقط أحمر ما بتبيّن على شيب صغير، بس على شاشة كاملة بتبيّن — والتطبيق صار يقرا بني.

والميلان اللوني **قرار**، ولما ينطبق على الخلفية بيصير قرار متكرر مئات المرات بكل شاشة وبدون أي مبرر. المحايد التام معناه إنو اللون الوحيد بالتطبيق هو لون إشي حطّه حدا عن قصد.

**الأحمر التحذيري مخصص للحذف وبس**، وبيختلف حسب الوضع: أحمر غامق كفاية ليُقرا على الورق بيصير موحل على شبه الأسود.

**التباعد بين الحروف متدرّج مع الحجم.** العناوين أضيق (`-0.8`)، النص العادي عادي. قيمة واحدة لكل الأحجام بتخلي العناوين تبيّن مفكّكة.

**ارتفاع السطر محدد صراحةً بكل مكان.** لو تُرك للخط، الإيقاع الرأسي بينحرف بين الشاشات وما بيتراصف إشي.

### 5.1 كيف اشتغل الوضع الفاتح/الغامق (Light/Dark Mode)


هاي نقطة تقنية مهمة تستاهل شرح، لأنها حل غير معتاد بس عملي:

بالمشروع، كل الويدجتس المخصصة بتستخدم `AppColors.background`, `AppColors.surface`... مباشرة (مش عبر `Theme.of(context)` متل الطريقة "الرسمية" بفلاتر). عشان نضيف دعم الوضع الفاتح بدون ما نعيد كتابة عشرات الملفات، حوّلنا `AppColors` من قيم ثابتة (`static const`) لـ **getters** بتقرأ متغير "سطوع" (`Brightness`) عام:

```dart
static Brightness _brightness = Brightness.dark;
static void setBrightness(Brightness brightness) => _brightness = brightness;
static Color get background => isDark ? const Color(0xFF0B0B0F) : const Color(0xFFF6F6FB);
```

وبـ [`main.dart`](lib/main.dart)، أول شي بالـ `build()` (قبل ما نبني أي widget تاني) منحدد السطوع الفعلي (حسب اختيار المستخدم System/Light/Dark، أو سطوع نظام التشغيل لو System)، ومنادي `AppColors.setBrightness(...)` **قبل** ما نبني `MaterialApp.router`. بما إنو فلاتر بيبني الشجرة بالترتيب (خطوة خطوة، مش بالتوازي)، أي widget تحت هالنقطة بالإطار (frame) نفسه بيشوف القيمة الصحيحة.

- [`theme_controller.dart`](lib/core/theme/theme_controller.dart): `ChangeNotifier` بسيط بيخزن اختيار المستخدم (`ThemeMode`) ويحفظه بـ `shared_preferences` (يضل بعد إغلاق التطبيق). مسجّل بـ `sl` كـ singleton واحد يشتركوا فيه `main.dart` (يقرأ منه) وSettings (يكتب فيه).
- **مهم لو رح تضيف widget جديد:** ما تستخدم `static const` لأي قيمة لونها من `AppColors` (غير `primary`/`secondary`/`error`/`success` الثابتين) — لازم تكون `final`/غير `const` عشان تلتقط تغيّر الوضع.

#### ليش تبديل الثيم كان "متعب للعين" — والحل بسطر واحد

المشكلة ما كانت السرعة. Material بيعمل **تلاشي تدريجي ٢٠٠ملي ثانية** لـ `ThemeData`، بينما كل الويدجتس المبنية بإيدنا بتقرا `AppColors` مباشرة وبتتبدل **فوراً**. فكان في ٢٠٠ ملي ثانية الشاشة **نصها فاتح ونصها غامق**.

يعني الإرهاق جاي من **اختلاف النصفين**، مش من السرعة. الحل:

```dart
themeAnimationDuration: Duration.zero,   // main.dart
```

الشاشة كلها بتتبدل بإطار واحد متّسق. وكمان `SystemChrome.setSystemUIOverlayStyle` بينحدّث بنفس اللحظة عشان شريط الحالة وشريط التنقل يتبعوا الثيم.

### 5.2 انتقالات سلسة بين الشاشات

[`fade_slide_page_route.dart`](lib/core/routes/fade_slide_page_route.dart): `PageRouteBuilder` بسيط بيعمل fade + انزلاق خفيف بدل الانتقال الافتراضي المفاجئ لأندرويد.

#### فتح الصورة — ليش كان مش سلس، وشو صار

الصورة أصلاً بتطير من البلاطة يلي ضغطت عليها للشاشة الكاملة (`Hero`). المشكلة ما كانت بالطيران نفسه — كانت **بالشغل يلي بيصير فوقه بنفس الـ 280ms**. أول ما تنفتح الصفحة كانت تبدأ ثلاث أشياء دفعة وحدة:

1. قراءة الملف **بدقته الكاملة** من التخزين وفك ترميزه — والسكرين شوت ملتقط بدقة الشاشة نفسها، يعني أكبر صورة ممكن الجهاز ينتجها.
2. بناء ثلاث ألواح ضبابية (`BackdropFilter`) — أغلى widget بفلاتر — بتضبّب صورة كاملة الشاشة **بكل إطار** من إطارات الحركة.
3. الصفحة نفسها كانت تنزلق ٤٪ لفوق بينما الصورة ماشية بمسار ثاني — حركتين مختلفتين بنفس اللحظة.

الحل قاعدة وحدة: **ما في إشي يصير غير الصورة لحتى تحط**.

- [`photo_viewer_route.dart`](lib/core/routes/photo_viewer_route.dart) — مسار خاص للعارض: بدون انزلاق، وبيخلّص الـ fade خلال أول ٥٥٪ من الوقت، فالخلفية بتستقر سودا قبل ما توصل الصورة. إشي إشي، مش كلشي مع بعض.
- `_settled` بـ `ScreenshotDetailPage` — بيصير `true` لما تنتهي حركة المسار. قبله: ما بتنقرا الصورة الأصلية ولا بتتبنى الأشرطة الضبابية أصلاً. بعده: الصورة بتوضح بتلاشي هادي، والأشرطة بتدخل بحركتها.
- [`photo_hero.dart`](lib/core/widgets/photo_hero.dart) — الـ `Hero` بيرفع الصورة لطبقة الـ overlay، وهي برّا الـ `ClipRRect` تبع البلاطة، فالزوايا المدوّرة كانت تصير حادة بأول إطار وترجع تدوّر بآخر إطار بالرجوع. صار كل طرف بيعلن نصف قطره والطيران بيتنقّل بينهم، فالزاوية بتنفتح مع الصورة.
- شريط الحالة وشريط التنقل تبع النظام: العارض أسود بالوضعين، بس الأشرطة كانت لسا ملوّنة حسب الثيم — بالوضع الفاتح كان يطلع شريط أبيض تحت الصورة وأيقونات غامقة مش بيّنة على الأسود. انحلّت بـ `AnnotatedRegion<SystemUiOverlayStyle>` بالعارض، ووحدة بجذر التطبيق بـ `main.dart` عشان ترجع الأشرطة لحالها لما تسكّر.

الاختبارات: [`test/photo_viewer_transition_test.dart`](test/photo_viewer_transition_test.dart) — بتثبّت إنو الصورة بتحط بالضبط بمكانها النهائي بدون قفزة، إنو الصفحة ما بتتحرك، وإنو الزاوية بتنفتح خلال الطيران.

### 5.3 الحركة والأداء

#### سبب التقطيع وقت التمرير — والحل

كانت كل بلاطة صورة وكل كرت فولدر فيهم:

```dart
.animate().fadeIn(duration: 220.ms).slideY(...)
```

هاد **بينفّذ على كل إعادة بناء**. وبالشبكة، البلاطات بتتدوّر (recycle) باستمرار وقت التمرير — فكل بلاطة بترجع للشاشة بتعيد الأنيميشن من الصفر وبتفتح `AnimationController` جديد. النتيجة: رفرفة + تقطيع.

**القاعدة:** حركة الدخول بتخص **الشاشات**، مش عناصر القوائم.

#### جولة أداء على التطبيق كله — وين كان بيعلق فعلياً

أربع مشاكل، كلها من نفس النوع: **شغل بينحسب على كل إطار، أو شغل بينعمل قبل ما حدا يطلبه.**

**١. الضبابية (blur) اللي ما بتوقف.** الـ `BackdropFilter` أغلى إشي بيرسمه فلاتر، والتكلفة **مش مرة وحدة** — لازم يعيد التضبيب بكل إطار بتتغير فيه البكسلات اللي وراه. شريط التنقّل السفلي كان `sigma: 30` وهو **ظاهر على الشاشة طول الوقت وفوق كل إشي بتعمله سكرول**، يعني كل تمريرة بالتطبيق كانت بتدفع حسابه. صار في [`glass_layer.dart`](lib/core/widgets/glass_layer.dart) مقياس واحد (`AppBlur.bar` ١٤ / `panel` ١٦ / `dialog` ١٢) وكل ضبابية بالتطبيق بتمرق منه — والشريط نزل لـ١٤ وزادت عتامة تعبئته بالمقابل، فبيبيّن نفس الإشي وبيكلّف أقل. **المقياس مدرّج حسب "قدّيش السطح ظاهر"، مش حسب شو أحلى بالصورة.**

**٢. التطبيق كان يبني الأربع تابات كلها قبل أول إطار.** `IndexedStack` بيحط كل أولاده بالشجرة فوراً — يعني فتح Shoto كان يبني المكتبة (وبتبدأ تفك ترميز صور محدا طلبها)، والفولدرات، **والإعدادات — اللي بتمشي على مجلد الكاش بالقرص عشان تحسب حجمه**. صار [`LazyIndexedStack`](lib/core/widgets/lazy_indexed_stack.dart): التاب ما بينبنى إلا لما تفتحه، وبعدها بيضل حي (وهاد مهم — عدّادات الفولدرات وموقع السكرول بالمكتبة معتمدين عليه).

**٣. فتح فولدر كان ٥٠ رحلة ذهاب وإياب بالطابور.** `getAssetsByIds` كان بينادي `AssetEntity.fromId` لكل صورة **وبيستنى كل وحدة تخلص قبل ما يبدأ اللي بعدها**. صاروا يتنفّذوا مع بعض (`Future.wait` بمجموعات ٢٤) — نفس الشغل للنظام، بس الانتظار صار بقدّر رحلة وحدة مش خمسين.

**٤. نقل صور محددة لفولدر كان ستين عملية قاعدة بيانات.** `assignFolder` كان بينادي upsert بحلقة: قراءة + كتابة لكل صورة، وكل وحدة بمعاملة (transaction) لحالها. صار **معاملة وحدة**، والقراءة انشالت أصلاً لأن `update` بيرجع كم صف تغيّر — والصفر هو بالظبط حالة "ما في صف بعد".

وكمان: صفحة البحث كانت تعمل `setState` بعد **كل** صورة تقرأها (لَـ٨٠ إعادة بناء كاملة للصفحة وسط أثقل شغل بالتطبيق) — صارت تنشر النتايج كل ٢٥٠ms.

الاختبارات: [`test/lazy_indexed_stack_test.dart`](test/lazy_indexed_stack_test.dart) بيثبّت النصين (ما بينبنى قبل ما ينفتح، وما بينرمى بعدها)، و[`test/goldens/nav_bar_*.png`](test/goldens) بيوروا إنو الزجاج لسا زجاج بعد التخفيف.

#### ليش صفحة الإعدادات كانت "بترجّح" وقت السكرول

انتبلغ عنها مرتين، والمرة الأولى انحلّت غلط — والسبب يستاهل يتكتب لأنو مش واضح:

**ردّ فعل الضغط جوّا أي قائمة قابلة للسكرول هو "تخمين" بطبيعته.** فلاتر ما بيعرف إذا الإصبع اللي نزلت رح تضغط ولا رح تسحب، فبيبلّغ عن الضغط أول (بعد ١٠٠ms على الأكثر) وبيسحب البلاغ لما السحب يفوز. **ما في أي callback بيقول "الضغطة فازت" قبل `onTap` نفسه** — يعني ما بتقدر تمنع البلاغ، بتقدر بس تخلي سحبه غير ملحوظ.

الحل الأول كان تأخير ٥٥ms قبل ما ينكمش الصف. هاد بس بيحرّك العتبة لـ~١٥٥ms: أي حدا بيريّح إصبعه لحظة قبل ما يسحب — وهاد بالظبط اللي بيصير بآخر صفحة طويلة — لسا بيشوف الانكماش وبعدين الرجوع، وصف ورا صف = الصفحة كلها بترجّ.

**الحل الصح: نوع ردّ الفعل نفسه.**
- `PressFeedback.scale` — انكماش. للأجسام **المنفصلة**: أزرار، شيبس، كروت، صور مصغّرة. إشي إله حافة وبيبيّن إنه ينضغط.
- `PressFeedback.highlight` — تظليل خفيف (٥.٥٪) **وما بيتحرك ولا بكسل**. لصفوف القوائم الممتدة بعرض الشاشة.

وكمان: الضغطة ما بتبدأ أصلاً على قائمة **عم بتتحرك** (الإصبع اللي بيوقف انزلاقة مش عم يضغط إشي)، وبتنلغي أول ما يبدأ السكرول فعلياً.

**وانشال الـ `BouncingScrollPhysics`** من الثلاث أماكن اللي كانت مستخدماه (الهوم، شريط الأخيرة، الإعدادات). هاد كان النص التاني من الشكوى — "بترجّح كثير" — لأنه ارتداد آيفوني على جهاز أندرويد، وكمان كان مطبّق على شاشتين من أربعة يعني السكرول نفسه كان مختلف من شاشة لشاشة. صار الكل على سلوك المنصة.

الاختبارات: [`test/press_feedback_scroll_test.dart`](test/press_feedback_scroll_test.dart).

#### `PressableScale` بدل ripple

بدل تأثير Material المموّج، كل عنصر قابل للضغط بينكمش شوي. ثلاث أسباب:
1. الـ ripple لازم ينقص (clip) عشان يزبط مع الكروت المدوّرة، وبالدارك مود بيبيّن لطخة رمادية.
2. الانكماش مقروء على أي سطح — صورة، تدرّج، أو لون عادي.
3. **بيبدأ لحظة ما تنزل إصبعك** (`onTapDown`) مش لما ترفعه — وهاد أهم سبب بيخلي التطبيق يحس سريع.

وبيحترم إعداد "Haptic feedback" من الإعدادات، بقراءة وقت الضغط مش مخزَّنة، فالتغيير بيسري فوراً.

#### الأون بوردنغ كان بيقطّع كمان

الخلفية المتحركة كانت `Timer.periodic(16ms)` بينادي `jumpTo`. المشكلتين:
1. **مش متزامن مع تحديث الشاشة** — التحديثات بتوصل بلحظات غلط فالحركة بترتجف.
2. **خطوة ثابتة بالبكسل** — يعني على شاشة 120Hz بتمشي **ضعف السرعة**.

الحل: `Ticker` (متزامن مع الإطار) + سرعة بالبكسل/الثانية بدل بكسل/تحديث.

### 5.3b الأون بوردنغ — "الكومة بتصير مكتبة"

الشاشة القديمة كانت خمس صور ستوك بتزحف ورا لوح مضبّب، ونص بيتلاشى فوقها. مشكلتها الأكبر ما كانت الشكل: **كانت بتوعد بإشي التطبيق ما عاد يعمله** — "بيلاقي كل سكرين شوت بتاخده تلقائياً" — وهاد انعكس من ٣ جلسات لما صارت المكتبة **اختيارية**. يعني أول جملة بيقراها المستخدم الجديد هي بالظبط الإشي اللي التطبيق قرر ما يعمله.

**الفكرة الجديدة: نفس التسع كروت من أول خطوة لآخر وحدة.** ما في صفحات بتتبدل ولا صور بتتلاشى — في تسع "سكرين شوتات" **مرسومة** (مش مصوّرة) بتبدأ ككومة مايلة ومتراكبة، وكل خطوة **بترتبهم قدامك**:

| الخطوة | شو بيصير للكروت | شو بتقول |
|---|---|---|
| ١ | كومة مايلة، متراكبة، طالعة برّا حدود الشاشة | ألف لقطة، كومة وحدة |
| ٢ | ست كروت بتطلع برّا، تلاتة بتتعدّل وتصطف | Shoto ما بيقرأ معرضك — إنت بتختار |
| ٣ | التلاتة بتتلمّوا فوق مجلد، **وبتاخد الزاوية المقصوصة** | بتنحفظ لحظة ما تبعتها |
| ٤ | كرت بيتقدّم وبيضوّي **سطر من نصه**، والباقي بيخفت | دوّر على اللي جوّاها |
| ٥ | الكروت بترجع لمروحة صغيرة، وكرت **بيغطّي رقم بطاقة** | نسخة البرو |

**ليش الكروت مرسومة مش مصوّرة؟** صورة لموبايل حدا تاني بتقرا كإعلان قبل ما تقرا كلمة. الكرت المرسوم بيقدر يعمل إشي الصورة ما بتقدر: يضوّي السطر اللي البحث لقاه، يغطّي الأرقام اللي Safe Share بتغطّيها، وياخد الزاوية المقصوصة بالظبط باللحظة اللي صار فيها ملك Shoto. **الأون بوردنغ بتمثّل التطبيق على نفس الإشي اللي بتحكي عنه.** وكمان ما بتكلّف ولا إشي: كم مستطيل، بدون فك ترميز صور ولا ضبابية — على الشاشة الوحيدة اللي بينبنى فيها الانطباع الأول والمحرك لسا عم بيسخن.

**قائمة البرو بتنقرا من [`PremiumFeature.all`](lib/features/subscription/domain/entities/premium_feature.dart)** — نفس القائمة اللي بتبني منها صفحة الدفع وكرت الإعدادات. أون بوردنغ إلها نسختها الخاصة من القائمة رح تصير غلط أول ما تضيف ميزة.

**البنية:** الشاشة القديمة كان وراها data source + repository + use case + bloc عشان تجيب خمس جمل من ملف JSON — والجمل أصلاً لازم تكون مترجمة. انشالوا كلهم؛ النصوص صارت بملفات ARB مع باقي لغات التطبيق (٦ لغات)، وصور الستوك والـ JSON انشالوا من الحزمة. الصفحة هلأ **بتنبنى بدون service locator أصلاً** — عشان هيك صار فيه [`test/onboarding_golden_test.dart`](test/onboarding_golden_test.dart) بيرسم الخمس خطوات بالوضعين.

### 5.4 شاشة البداية (Splash)

على مرحلتين عشان ما يصير أي وميض:
1. **أصلية (Android)**: [`launch_background.xml`](android/app/src/main/res/drawable/launch_background.xml) بلون العلامة + الشعار كـ vector، وملفات `values/` و`values-night/` منفصلة. اللون **مطابق تماماً** لـ `AppColors.background` بكل وضع.
2. **بفلاتر**: [`splash_page.dart`](lib/features/splash/presentation/pages/splash_page.dart) بيبدأ نسخة طبق الأصل من الشاشة الأصلية وبعدين **بيتحرك خارج منها**.

وكمان صار مسار البداية `/splash` بدل `/onboarding` — فالمستخدم المسجّل ما عاد يشوف ومضة من شاشة الترحيب قبل ما ينقله الراوتر.

### 5.5 إعادة هيكلة الهوم بيج — من معرض لمركز

**المشكلة:** الهوم كان شبكة كل الصور — يعني نسخة من معرض الجوال. فتحه كان بيجاوب على سؤال *"شو عندي؟"*، وهاد المعرض بيجاوبه أصلاً، بدل *"شو لازم أعمل؟"*.

**الحل:** الشبكة انتقلت لتاب **Library** خاص فيها، والهوم صار مركز:

| القسم | وظيفته |
|---|---|
| **بطاقة الترتيب** | البطل الوحيد بالشاشة: "٢٣ بدها ترتيب" |
| **٣ إحصائيات** | صور، مفضلات، فولدرات |
| **شبكة الأدوات** | كل ميزة بالاسم والوصف — مدفوعة كانت أو مجانية |
| **شريط آخر الصور** | يكفي تتعرّف إن أغراضك هون، بدون ما يرجع معرض |

#### رقم "بدها ترتيب" **مشتق**، مش مخزَّن

```dart
all.where((s) => s.folderId == null && !s.isFavorite).length
```

أي صورة لا مرتّبة بفولدر ولا مفضّلة هي — **بالتعريف** — لسا مستنيّة المستخدم. اشتقاقه بدل تخزينه يعني **مستحيل يكون غلط**، بينما عمود مخزَّن ممكن ينحرف عن الواقع.

#### الـ Bloc انتقل لمستوى الـ Shell

`ScreenshotsBloc` صار محمول بـ `MainShellPage` بدل ما كل شاشة تعمل نسخة. الهوم بيقرا منه الأعداد والـ Library بيرسم الشبكة — **تحميل واحد بيغذّي الاثنين**، ومستحيل يختلفوا على محتوى المكتبة. وتبديل التابات صار ببلاش.

### 5.6 توضيح الميزات المدفوعة

[`premium_feature.dart`](lib/features/subscription/domain/entities/premium_feature.dart) — **مصدر واحد للحقيقة** عن شو بتحصّل لما تدفع.

المشكلة كانت: كل ميزة مدفوعة انبنت بوقتها، وشاشة الاشتراك ضلّت تعرض **٣ نقاط عامة** مكتوبة قبل ما معظمهم يوجد. يعني اللي بيوصلها ما بيعرف إن التطبيق بيقرا نص الصور، ولا بيدمج اللقطات الطويلة، ولا بيلاقي المكرر.

هلق الشاشة بتعرض **القائمة الحقيقية كاملة**، وكرت الإعدادات بيقرا من نفس المصدر. إضافة ميزة مدفوعة = إضافتها بهالملف بس.

#### باگ حقيقي: الحارس كان معكوس

بمكانين (Folders + Settings) كان مكتوب:

```dart
if (status.isPremium) {  ...يعرض شاشة الدفع... }
```

يعني **المشترك** بتطلعله شاشة الدفع، و**المجاني** بيفوت ببلاش. جمعت كل الفحوصات بملف واحد:

```dart
// lib/core/widgets/premium_gate.dart
Future<bool> ensurePremium(BuildContext context)
```

غلطة مستحيل تتكرر لما يكون في تطبيق واحد بس — وهي بالظبط النوع اللي ما بينكشف إلا بالإنتاج.

### 5.6b الاهتزاز (Haptics) — ليش ما كان بينحس

المستخدم شغّل خيار "Haptic feedback" وما حسّ بإشي. الكود كان شغال، والإعدادات على الجهاز كانت **مفعّلة وعلى أقوى شدة**. المشكلة كانت أعمق.

#### الدليل

`adb shell dumpsys vibrator_manager` بيحتفظ بسجل آخر الاهتزازات. طلبات Shoto كانت موجودة فيه — وكلها هيك:

```
effect: Prebaked{effect=TEXTURE_TICK, fallback=true}
status: ignored_unsupported      ← النظام رماها
```

يعني الطلب كان بيوصل للنظام، والنظام **بيتجاهله**.

#### السبب

كل دوال `HapticFeedback` بفلاتر هي نداء لـ`View.performHapticFeedback` مع ثابت من `HapticFeedbackConstants`. وهدول الثوابت **اقتراحات مش أوامر**: كل شركة بتقرر شو بيقابلها على أجهزتها، وكتير منها ما بيقابله ولا حركة. و`selectionClick()` (اللي كان مستخدم بكل ضغطة عن طريق `PressableScale`) بيقابله `TEXTURE_TICK` — وهاد بالظبط اللي جهاز المستخدم بيرفضه.

#### الحل: نشغّل المحرك مباشرة

[`HapticsChannel.kt`](android/app/src/main/kotlin/com/shoto/app/HapticsChannel.kt) — قناة `shoto/haptics` بتستخدم `Vibrator` مباشرة، فالتأثير اللي بنطلبه هو التأثير اللي بينعزف. كلفتها إذن `VIBRATE` (إذن عادي، بينعطى وقت التركيب وما بيسأل المستخدم) وملف واحد.

بترتيب أفضلية:
1. **تأثير الشركة الجاهز** (`createPredefined`) لو `areEffectsSupported` قالت "نعم" صراحةً — هاد الأحلى إحساس لأنه معاير على المحرك نفسه.
2. **نبضة صريحة** (`createOneShot`) لو لأ.

#### التفصيلة اللي فرقت فعلياً: نوع المحرك

في نوعين محركات، وعاملناهم زي بعض بالبداية — وهاد كان السبب إنه ضل ضعيف حتى بعد أول إصلاح:

| المحرك | كيف بيتصرف | شو بنعمل |
|---|---|---|
| **LRA** (خطي) | بيوقف وبيشتغل فوراً، وبيدعم تدرّج شدة | نبضات قصيرة (١٠–٣٢ ملي)، والفرق بالشدة |
| **ERM** (كتلة دوّارة) | لازم **يدور** فيزيائياً، وما بيدعم شدة إطلاقاً | نبضات أطول (٢٤–٧٠ ملي)، لأن المدة هي الدولاب الوحيد |

جهاز المستخدم ERM (`hasAmplitudeControl() == false`, `mSupportedEffects=[]`). نبضة بطول ١٤ ملي عليه بتخلص **قبل** ما الكتلة تتحرك كفاية عشان تحسها.

#### النتيجة، متحقَّق منها على الجهاز

```
effect: Step{amplitude=1.0, duration=49}
status: finished              ← انعزفت فعلاً
```

#### وكمان

- **البوتوم نافيقيشن ما كان فيه اهتزاز أصلاً** — وهاد كان أكتر إشي المستخدم لاحظه ("لما انتقل"). صار فيه، **بس لما التاب يتغير فعلاً**: اهتزاز لما ما بيصير إشي بيعلّم المستخدم يتجاهل الإحساس.
- كل نداء مباشر لـ`HapticFeedback` بالتطبيق انلغى — كلهم بيمرّوا عبر [`Haptics`](lib/core/services/haptics.dart) هلأ، فالتفضيل بينفحص **بمكان واحد**. قبل هيك كان في ٦ أماكن بتتجاوزه، يعني إطفاء الخيار ما كان بيطفّي كل إشي.
- **مفتاح الإعداد بيهتز وقت ما تشغّله** (`Haptics.demo()`، بيتجاهل التفضيل عن قصد) — مفتاح إلو تأثير بمكان تاني ووقت تاني هو مفتاح الناس بتفترض إنه معطّل، وهاد بالظبط اللي صار.
- iOS ضل على `HapticFeedback` — الـTaptic Engine ممتاز وواجهات آبل معايرة عليه.

### 5.7 خيارات الإعدادات

[`app_preferences.dart`](lib/core/services/app_preferences.dart) بيحفظ: **الاهتزاز عند الضغط** و**السؤال قبل الحذف**.

كان في كمان خيار "الفتح على Library مباشرة" — **انحذف**. المستخدم طلب إن التطبيق يفتح دايماً على الهوم، وخيار بيغيّر أول شاشة بتشوفها هو خيار بتضبطه مرة وبتنساه وبعدين بتستغرب ليش التطبيق بيتصرف غير عن المتوقع. شاشة بداية وحدة ثابتة أوضح من إعداد.

وتحت **Privacy** في نص واضح بيقول إن Shoto **ما بيقرأ معرضك** وإن كل معالجة بتصير على جهازك — ادعاء صار قابل للتحقق فعلاً بعد ٤.١٤، مش مجرد كلام تسويقي.

> **باگ اتصلّح:** `setState(() => _future = someAsyncCall())` — السهم بيرجّع **قيمة الإسناد** واللي هي `Future`، وFlutter بيرمي استثناء لأن دالة `setState` ممنوع ترجّع Future. كان بيضرب عند الضغط على "Upgrade"، ونفس النمط كان موجود بـ "Clear cache". الحل: قوس بلوك `{}` بدل السهم — إنشاء الـ Future متزامن، **قيمة الإرجاع بس كانت المشكلة**.

---

### 5.8 التذكيرات — المنبّه اللي كان ناقص النية ⏰

من زمان والتطبيق بيعرف **شو كنت ناوي تعمل** بالسكرين شوت: بتختار الفعل بضغطة وحدة وقت ما بتحفظه (buy / pay / reply…)، وكل فعل بالقائمة إشي بينخلص بمرحلة ما. بس التطبيق كان بيعرف يرجّعلك هالمعلومة **بس لما تفتحه وتدوّر**.

يعني قائمة مهام بلا منبّه. وبتفشل بالطريقة الأسوأ تحديداً: **السكرين شوتات اللي بدها تذكير أكتر إشي هي بالظبط اللي محدا رح يفتح التطبيق عشان يلاقيها.** فاتورة صوّرتها وإنت واقف عالباص ما بترجعلك لأنك "تصفحت المكتبة".

#### وين "بعدين" بالظبط

[`reminder_times.dart`](lib/core/utils/reminder_times.dart) — دارت صافي بلا Flutter، نفس التقسيمة تبع الـ stitcher والـ hashes. وهون بتستاهل فعلاً: كل قاعدة إلها **حافة بعرض ساعة**، والطريقة الوحيدة تتأكد إنها صح هي إنك تسألها من الجهتين.

أربع خيارات جاهزة، كلها نسبية، لأن هاد بينستخدم بإيد وحدة بالثانية اللي بعد ما تقرر تأجل الإشي.

**والساعة المسمّاة بتكون دائرية، والمسافة المقاسة بتكون مضبوطة.** "هالمسا" = السابعة، لأن السابعة هي معنى الكلمة نفسها. بس "بعدين اليوم" = **تلات ساعات بالظبط**، وتلات ساعات من 16:22 هي 19:22.

> **باگ انصلّح بعد ما المستخدم لاحظه على الجهاز:** كان "بعدين اليوم" بيجبر النتيجة لساعة كاملة **لفوق**، فمن 16:22 طلع **20:00** — يعني ٣ ساعات و٣٨ دقيقة، زيادة ٢١٪ على الرقم الوحيد اللي الزر بيلمّح إله. الحجة كانت "20:00 وقت بتقدر ترتب حالك عليه" — وهاي صحيحة لوقت **المستخدم بيسمّيه**، وغلط لوقت **التطبيق بيحسبه**. تدوير مسافة نسبية هو التطبيق عم بيصرف من وقت المستخدم على ذوقه بشكل الساعة.
>
> وانضاف معها قاعدة تانية بنفس المنطق: **"بعدين اليوم" لازم يضل اليوم.** تلات ساعات من 22:30 هي 01:30 بكرا — إزاحة صحيحة تحت لافتة كاذبة، وهاد إشي بتكتشفه لما يوقظك. صار ما بينعرض إذا النتيجة طلعت من اليوم، تماماً زي "هالمسا" لما بتعدّي.

وكل خيار مكتوب جنبه الوقت اللي بينحل عليه، فما في نوع منهم تخمين لازم القارئ يعمله.

**والخيار اللي كلامه بطّل صحيح بينشال من الشيت، ما بينعاد تعريفه بالسر.** "هالمسا" لو ضغطتها الساعة ١١ بالليل بتطلع بكرا مسا — وهاد صح منطقياً ومش اللي مكتوب عالزر. فما بينعرض بعد الساعة ٦. التلاتة التانية صادقين بأي ساعة، فدايماً موجودين.

**١٢ اختبار**، منهم مسح على كل خيار بكل ساعة من اليوم بيتأكد من الخاصية اللي أهم من أي جواب لحاله: **التذكير أبداً ما بينحل على وقت ماضي**. لأن اللي بينحل عالماضي إما بيرن فوراً أو ما بيرن أبداً، وأي وحدة فيهم بتعتمد على مين انتبه أول.

#### منبّه غير مضبوط — عن قصد

بنستخدم `setAndAllowWhileIdle` مش `setExactAndAllowWhileIdle`. النسخة المضبوطة بدها إذن `SCHEDULE_EXACT_ALARM`، واللي أندرويد ١٢ خلاه إذن خاص وجوجل بلاي بتراجعه مقابل لستة قصيرة من الاستخدامات المشروعة — منبّهات وتقاويم. "ذكّرني بهالسكرين شوت المسا" مش منهم، وطلبه بيكون طلب إشي الميزة أصلاً مش محتاجته: محدا عم يوقّت بيضة.

**والمنبّهات بتنمسح لما الجهاز يعيد التشغيل.** إعادة ضبطها بدها معرفة شو كان معلّق — قبل ما Flutter تشتغل بوقت طويل، ومن عملية ما بتقدر توصل لملف sqflite بعالم دارت. عشان هيك الجدولة بتكتب كمان **نسخة صغيرة** بالتفضيلات تبع الـ scheduler نفسه، و`BootReceiver` بيعيد التسليح منها. نفس التقسيمة اللي `CaptureAlertsChannel` موثّقها: **دارت بتملك اللي المستخدم طلبه، والمنصة بتملك اللي فعلاً مسلّح**، وما في واحد فيهم بينسأل سؤال التاني.

والتذكير اللي عدّى وقته والجهاز مطفي **بينحذف، ما بينطلق متأخر**. قيمة "هالمسا" كلها إنه يوصل هالمسا؛ واللي بيوصل وقت ما الجهاز صحي هو ضجيج المستخدم ما بيقدر يحطه بمكانه. الصف بقاعدة البيانات بيضل، فالتطبيق بيقدر يقول إنه انفات.

#### تفاصيل باقية

- **`remind_at` عمود لحاله** (سكيما ٢١)، مش مدموج مع الـ intent اللي عادةً بينحط معه. بينمسحوا بأحداث مختلفة — الـ intent بينتهي لما تقول عملت الإشي، والتذكير بينصرف لحظة ما يرن — وعمود واحد كان بيخلي تذكير رن يبيّن كأنه مهمة خلصت، أو مهمة خلصت تضل ترن.
- **الصف والمنبّه بينكتبوا سوا بالـ repository**، وبهاد الترتيب: لو التسليح فشل، الصف لسا بيقول في تذكير — وهاد النص اللي بينستعاد.
- **لما تحط تذكير، بيقلك إذا فعلاً رح تشوفه.** أندرويد بيرمي الإشعار بصمت لو إشعارات Shoto مطفية، فالتأكيد جملتين مختلفتين — لأن أسوأ وقت تكتشف فيه الفرق هو لما ما تنذكّر.
- **الضغط على الإشعار بيفتح نفس السكرين شوت**، وقت الإقلاع ووقت الرجوع من الخلفية. `MainActivity` هي `singleTop`، فالضغطة والتطبيق شغال ما بتمر من `onCreate` أبداً — وبدون مسار الـ resume كان الإشعار بس بيجيب التطبيق قدام على أي شاشة كانت، يعني تذكير رن وما عمل إشي.
- نص الإشعار بينتركب من ملفات ARB، فبيحكي لغة **التطبيق**. بس اسم القناة ووصفها من `strings.xml`، لأن أندرويد بيرسمهم بإعداداته هو بدون ما يكون التطبيق شغال.
- على iOS الميزة **مقفولة صراحة** بدل ما تشتغل بالفراغ — `Reminders.isSupported` هو اللي بيخلي الشيت يقول هيك.

---

## 6. قاعدة البيانات المحلية (sqflite)

معرّفة بملف [`app_database.dart`](lib/core/database/app_database.dart) — اتصال واحد مشترك لكل التطبيق، **الإصدار الحالي ١٠**:

```sql
CREATE TABLE folders (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id TEXT NOT NULL,            -- Firebase UID تبع صاحب الفولدر
  name TEXT NOT NULL,
  color INTEGER NOT NULL,
  is_private INTEGER NOT NULL DEFAULT 0,
  created_at INTEGER NOT NULL
);

CREATE TABLE screenshot_meta (
  user_id TEXT NOT NULL,            -- Firebase UID
  asset_id TEXT NOT NULL,           -- id الصورة بالمعرض (مش الصورة نفسها!)
  folder_id INTEGER,                -- فولدر اختياري
  is_favorite INTEGER NOT NULL DEFAULT 0,
  ocr_text TEXT,                    -- نص الـOCR المخزّن (v2) — للبحث
  phash TEXT,                       -- بصمة إدراكية dHash (v5) — للمكرر
  category TEXT,                    -- ⚠️ ميت: بقايا Smart Albums
  visual_labels TEXT,               -- ⚠️ ميت: بقايا Smart Albums
  category_override TEXT,           -- ⚠️ ميت: بقايا Smart Albums
  updated_at INTEGER NOT NULL,
  PRIMARY KEY (user_id, asset_id),
  FOREIGN KEY (folder_id) REFERENCES folders (id) ON DELETE SET NULL
);

CREATE TABLE library_assets (          -- v10: مين بيملك كل صورة
  user_id TEXT NOT NULL,               -- Firebase UID
  asset_id TEXT NOT NULL,              -- id الصورة بألبوم Shoto
  added_at INTEGER NOT NULL,
  PRIMARY KEY (user_id, asset_id)
);

CREATE TABLE app_flags (               -- مفاتيح داخلية، مش إعدادات مستخدم
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL
);
```

#### `library_assets` — ليش هاد الجدول هو أهم شي بالعزل بين الحسابات

كانت في مشكلة حقيقية: **تسجّل دخول بحساب تاني، بتشوف صور الحساب الأول كلها.**

السبب مش bug بسطر معيّن — سبب **معماري**. صور SHOTO بتنحفظ بمجلد واحد على الجهاز: `Pictures/SHOTO`. ومجلد على القرص ما بيعرف شي اسمه "حسابات". فلما `getScreenshotAssets()` كانت تقرأ محتويات الألبوم وترجّعها، كانت فعلياً تقول: "المكتبة = كل شي بالمجلد" — لأي حدا مسجّل دخول.

الـ`user_id` كان موجود من قبل، بس على **بيانات التنظيم بس** (فولدر، مفضّلة، OCR). يعني اليوزر التاني ما كان يشوف فولدراتك — بس كان يشوف صورك، فاضية من التنظيم.

الحل إنه **العضوية بالمكتبة صارت قرار بيتخزّن**، مش نتيجة لوجود الملف:

```dart
// screenshot_repository_impl.dart
final assets = await _gallery.getScreenshotAssets();   // كل الألبوم
final owned  = await _ownership.getOwnedAssetIds();    // اللي بيملكه هاد الحساب
final mine   = assets.where((a) => owned.contains(a.id)).toList();
```

الصورة بتدخل مكتبتك لما **إنت** تستوردها (`claim`)، وبتطلع لما تحذفها (`release`). وكل مسار بيحط صورة بالمعرض لازم يمرّ من `ScreenshotRepository` — عشان هيك `StitchRepositoryImpl` (دمج الصور الطويلة) اتغيّر يستعمل الـrepository بدل ما يكتب بالمعرض مباشرة، لأنه كان بيتخطّى الملكية.

**الحذف حالة خاصة.** الملف واحد ومشترك، فحذفه من مكتبتك ما لازم يحذفه من مكتبة غيرك:

```dart
await _ownership.release(assetIds);                       // اطلع من مكتبتي
final keptByOthers = await _ownership.ownedByOthers(ids); // حدا تاني لسا بدّه ياها؟
// الملف بينحذف فعلياً بس إذا ما ضل حدا بيملكه
```

**حدود الحل (مهم تعرفها):** هاد عزل **جوّا التطبيق**، مش تشفير. الملفات لسا بمعرض الجهاز، ويلي يفتح تطبيق الصور بيشوفها كلها. إخفاؤها كلياً معناه إنها تبطّل تكون صور معرض حقيقية — وهاد شي كل مسارات المشاركة والحذف والدمج مبنية عليه.

#### الصور القديمة — لمين بتروح؟ (ترقية v10)

الصور اللي كانت بالألبوم **قبل** ما تصير الملكية تتسجّل ما عندها مالك بالبيانات. التعامل معها كان أصعب قرار بالترقية:

1. **الدليل الوحيد المقبول هو تنظيم متعمّد:** صورة محفوظة (`is_favorite = 1`) أو مصنّفة بفولدر (`folder_id IS NOT NULL`) — هاد شي الشخص عمله بإرادته، و`screenshot_meta` بتسجّل أي حساب عمله.
2. **باقي صفوف `screenshot_meta` مش دليل ملكية أبداً** — البحث بيكتب صف لتخزين نص الـOCR، وفحص المكرر بيكتب صف لتخزين البصمة، **بمجرد إنك فتحت المكتبة**. هاي كاش، مش ملكية.
3. الباقي بينحطّ لأول حساب بيفتح المكتبة بعد الترقية، **مرة وحدة على هاد الجهاز**، وبعدين `app_flags.legacy_library_adopted = '1'` وما بتنعاد أبداً.

النقطة ٢ كانت **غلط بأول محاولة**: الترقية الأولى نقلت *كل* صفوف `screenshot_meta`. وقت فحصنا الجهاز الحقيقي طلع إنه ٧٨ صورة صار عندها ٣ ملّاك — لأن الحسابات التلاتة تصفّحوا نفس المكتبة أيام الـbug، فانكتبلهم كاش OCR عليها. يعني الترقية كانت هي نفسها بتعيد إنتاج المشكلة يلي جاية تحلّها. الأرقام الحقيقية بعد التصحيح: ٢ و١ صورة منظّمة فعلياً، مقابل ٧٧ و٨١ و٧٣ صف كاش.

**الدرس:** لما تنقل بيانات قديمة، اسأل عن كل عمود *"شو الشي اللي أكيد بيثبته؟"* — لا *"شو الشي اللي غالباً بيدلّ عليه؟"*. والفحص على جهاز فيه بيانات حقيقية هو يلي كشف الفرق، مش قراءة الكود.

#### ليش أعمدة ميتة موجودة أصلاً؟

بعد حذف Smart Albums (٤.٩) ما في كود بيقرأ ولا بيكتب `category` و`visual_labels` و`category_override`. وبرضو ضلّوا موجودين، **بقصد**:

الترقيات كلها `ALTER TABLE ... ADD COLUMN` — إضافية بحتة، ما بتلمس بيانات موجودة. لو حذفناهم من `CREATE TABLE`، بيصير عنا **نوعين قاعدة بيانات**: جهاز مركّب التطبيق من زمان (عنده الأعمدة من الترقية) وجهاز مركّبه اليوم (بدونها). وSQLite ما بيدعم `DROP COLUMN` قبل ٣.٣٥، يعني تنظيفهم فعلياً بدّه إعادة بناء الجدول كامل مع نقل البيانات — مخاطرة حقيقية على بيانات المستخدم مقابل ٣ أعمدة فاضية.

القاعدة: **عمود ميت أرخص من هجرة قاعدة بيانات.**

`ON DELETE SET NULL` معناها: لو حذفت فولدر، الصور اللي كانت فيه ما بتنحذف — بس بيصير `folder_id` تبعها `NULL` أوتوماتيكياً (SQLite بيعملها لحاله).

**كل الجداول فيها `user_id` (الـFirebase UID):** لو شخصين سجّلوا دخول بحسابين مختلفين على نفس الجهاز، كل واحد بيشوف **صوره** وفولدراته ومفضّلاته بس — مطبّقة بـ`WHERE user_id = ?` بكل query داخل `LibraryOwnershipLocalDataSource` و`ScreenshotMetadataLocalDataSource` و`FoldersLocalDataSource`. والـ`PRIMARY KEY (user_id, asset_id)` (بدل `asset_id` لحاله) مهم: هيك نفس الصورة ممكن تكون مفضّلة عند مستخدم وغير مفضّلة عند غيره.

**ملاحظة مهمة بعد ٤.١٤:** `asset_id` صار دايماً بيشير لصورة **جوا ألبوم Shoto**، مش لصورة عشوائية بالمعرض. الجدول ما تغيّر، بس معنى الصف تغيّر: صار بيوصف عنصر بمكتبتك، مش تعليق على صورة بمعرضك.

---

## 7. مشاكل حقيقية واجهناها وكيف انحلت

هاد الجزء مهم كتير للتعلّم — مو نظرية، مشاكل حقيقية صارت أثناء بناء هاد التطبيق بالضبط:

### 7.1 تعارض إصدار Java/Kotlin (Gradle)
**العرض:** `Inconsistent JVM Target Compatibility` — كل مكتبة أندرويد (`photo_manager`, `receive_sharing_intent`) عندها إعداد افتراضي مختلف لجافا/كوتلن.
**الحل:** بدل ما نضبط كل مكتبة لحالها، ضفنا **JVM Toolchain** موحّد بملف [`android/build.gradle.kts`](android/build.gradle.kts) بيجبر كل المكتبات تستخدم نفس الـ JDK لجافا وكوتلن سوا، فما بصير تعارض أصلاً.

### 7.2 نسخة مكتبة فيها Bug
`receive_sharing_intent: ^1.9.0` (أحدث إصدار وقتها) كان عندها خطأ حقيقي بملف الـ Gradle تبعها (ناسية تطبّق بلقن Kotlin رغم استخدامها). الحل: تثبيت نسخة أقدم (`1.8.1`) بعد التأكد إنها مضبوطة صح.

### 7.3 رسالة خطأ مضلّلة
كود قديم كان يحوّل أي خطأ فيه كلمة قريبة من "cancel" لرسالة "تم الإلغاء" — حتى لو السبب الحقيقي كان تاني تماماً (زي client ID خطأ). الدرس: لا تفترض سبب الخطأ من النص، افحص نوع الخطأ (`error.code`) وتعامل مع كل حالة صح.

### 7.4 ANR (التطبيق ما بيستجيب) عند التشغيل
فحصنا logcat مباشرة ولقينا السبب مكتوب بوضوح: أول فريم أخد 6.6 ثانية يترسم (debug mode + Firebase + كذا مكتبة native)، وأندرويد بيعتبرها ANR بعد 5 ثواني بدون نافذة. مش باغ بالكود — بطء إقلاع طبيعي بوضع Debug، وبيتحسن بـ `--profile`/`--release` أو زيادة رام الإيموليتر.

### 7.5 بيانات "عالقة" ما بتتحدث إلا بـ Hot Restart
عدد صور الفولدر ما كان يتحدث. السبب: `IndexedStack` بيحافظ عالتابات بالذاكرة، فالـ Bloc يلي جوا `FoldersPage` ما كان يعاد إنشاءه أبداً. الحل: نقلنا ملكية الـ Bloc لمستوى أعلى (`MainShellPage`) وصرنا نعيد تحميله يدوياً كل ما المستخدم يفتح تاب Folders.

### 7.6 الوضع الغامق ما كان ينطبق على كل الشاشات
مفتاح Light/Dark كان يشتغل، بس شاشات Home/Folders/Settings تضل بالوضع الأول يلي انفتح فيه التطبيق. السبب مش تصميم — كان **باغ حقيقي بسبب `const`**: بـ `MainShellPage` كانت التابات مكتوبة `children: const [HomePage(), FoldersPage(), SettingsPage()]`، وكلمة `const` معناها لفلاتر "هدول ثابتين ما بيتغيروا أبداً" فبيتخطى إعادة بنائهم كلياً — وهاي بالضبط وظيفة `const` (تحسين أداء). بس بما إنو ويدجتسنا بتقرأ `AppColors` مباشرة (مش عبر `Theme.of(context)`)، تخطّي إعادة البناء معناه إنها بتضل شايفة السطوع القديم للأبد. نفس الشي كان بـ `builder: (context, state) => const MainShellPage()` بالراوتر. **الحل:** حذفنا `const` من هالمواضع تحديداً.

**الدرس:** `const` مش دايماً تحسين مجاني — لو الويدجت بتعتمد على حالة عامة (global) مش ممرّرة عبر الـ constructor أو الـ `BuildContext`، الـ `const` بتكسر تفاعليتها.

---

## 8. شو لسا ناقص / خطوات جاية

- [x] ~~غيّر `DevAccess.enabled` لـ`kDebugMode`~~ — **انعمل**. صارت `kDebugMode || bool.fromEnvironment('DEV_UNLOCK')`، يعني نسخة release عادية بتشيل الفرع كله من الباينري، وما بتنفتح إلا لو حدا كتب `--dart-define=DEV_UNLOCK=true` بإيده.
- [ ] 🚨 **توقيع نسخة الـ release لسا بمفتاح الـ debug** — بملف [`build.gradle.kts`](android/app/build.gradle.kts) مكتوب حرفياً `signingConfig = signingConfigs.getByName("debug")` مع TODO. جوجل بلاي بترفض أي رفع موقّع بمفتاح debug، فهاد أول إشي لازم ينحل قبل النشر: keystore حقيقي + ملف `key.properties`.
- [ ] 🚨 **مفاتيح RevenueCat لسا placeholders** بملف [`subscription_constants.dart`](lib/core/constants/subscription_constants.dart) (`REPLACE_WITH_REVENUECAT_…`). التطبيق بيشتغل عادي بدونها وبيعرض أسعار احتياطية، بس ما في ولا اشتراك حقيقي ممكن ينشرى.
- [ ] 🚨 **بصمة SHA-1/SHA-256 لنسخة release ما انسجلت بفايربيس.** وهاي أخطر مما تبدو هون بالذات: تسجيل الدخول حائط إجباري، فلو البصمة ناقصة أول مستخدم بينزّل النسخة الحقيقية بيعلق على شاشة الدخول وما بيفوت أبداً.
- [ ] تفعيل "Sign in with Apple" فعلياً من Xcode + Apple Developer (الكود جاهز، بس محتاج إعداد حساب مطوّر أبل).
- [ ] تسجيل بصمة SHA-1/SHA-256 لنسخة الـ **release** بفايربيس (بصمة الـ debug مسجّلة، بس هاي مختلفة).
- [ ] عدد صور الفولدر بشاشة تفاصيل الفولدر نفسها (`FolderDetailPage`) تلقائي التحديث لأنها صفحة جديدة كل مرة — بس لو ضفنا صفحات تانية بالمستقبل بتعرض إحصائيات محفوظة بالذاكرة، لازم نطبّق نفس حل §7.5.
- [ ] لسا ما في ترقيم صفحات (pagination) لو صار عند المستخدم آلاف الصور — هلأ بنجيبهم كلهم دفعة وحدة. صارت أقل إلحاحاً بعد ٤.١٤ (المكتبة منتقاة مش كل المعرض)، بس لسا لازمة.
- [ ] شيت المشاركة (٤.١٣) موجود على أندرويد بس. مكافئه على iOS بدّه **Share Extension** — تارجت منفصل بـXcode مع App Group مشترك للوصول للبيانات، وهاد شغل ما بينعمل من كود دارت.
- [ ] بما إن المكتبة صارت اختيارية، لازم يكون في طريقة **استيراد بالجملة** (اختار من المعرض مرة وحدة أول ما تنزّل التطبيق) — وإلا المستخدم الجديد بيفتح تطبيق فاضي تماماً.

### وضع المطوّر — فتح الميزات المدفوعة للتجربة

لأن RevenueCat ما بيقدر يعطي اشتراك حقيقي قبل ما تفتح حسابات Apple/Google وتنشئ المنتجات، كل الميزات المدفوعة كانت **غير قابلة للوصول أصلاً** على جهاز حقيقي. عشان هيك في مفتاح محلي:

**آخر صفحة الإعدادات → اضغط على رقم النسخة ٣ ضغطات → اكتب `2580`.**

بعدها التطبيق بيتعامل معك كمشترك بالكامل. وللرجوع للنسخة المجانية: ٣ ضغطات كمان مرة (لازم تقدر ترجع، وإلا ما بتقدر تجرّب الـpaywall والحدود المجانية تاني).

**التصميم — نقطة واحدة بس:** الفحص كله بيمرّ من `SubscriptionRepositoryImpl.getStatus()`، فحطّينا الـoverride هناك بالضبط:

```dart
SubscriptionStatus _withDevAccess(SubscriptionStatus status) {
  if (status.isPremium) return status;   // الاشتراك الحقيقي دايماً أولاً
  return _devAccess.isUnlocked ? SubscriptionStatus.developer : status;
}
```

**ليش مكان واحد؟** لأن `ensurePremium` وحدّ الـ٥٠ صورة وحدّ الفولدرات وكرت الإعدادات كلهم بيقرأوا من `getStatus()`. تكرار الشرط بكل مكان هو بالضبط اللي خلّى شرط الـpremium ينكتب **مقلوب** مرتين قبل هيك (§٥.٦). وترتيب السطرين مقصود: الاشتراك الحقيقي بينفحص أول، عشان إطفاء وضع المطوّر ما يقدر أبداً يسحب شي حدا دفع فيه فعلاً.

وكرت الإعدادات بيكتب **"Developer unlock — not a real subscription"** مش "Shoto Premium" — لأن شاشة بتدّعي اشتراك ما انشترى هي بالضبط كيف بتشحن paywall ما جرّبته ولا مرة.

مثبّتة بـ١٣ اختبار: [`dev_access_test.dart`](test/dev_access_test.dart) للكود والحفظ، و[`dev_access_gating_test.dart`](test/dev_access_gating_test.dart) للبوابة نفسها.

### خطوات تفعيل الاشتراكات (Subscriptions) — لازم تسويها إنت بنفسك

1. افتح حساب [Apple Developer Program](https://developer.apple.com/programs/) ($99/سنة) و[Google Play Console](https://play.google.com/console/) ($25 مرة وحدة).
2. سجّل تطبيق Shoto بكل من App Store Connect وPlay Console. الـ package name صار نهائي: **`com.shoto.app`** (كان `com.example.shoto`) — ولازم يضل هيك، لأن بلاي بتعرّف التطبيق فيه بعد أول رفع وما بينغيّر بعدها.
3. أنشئ منتجين اشتراك بكل متجر: شهري بـ **$7.00** وسنوي بـ **$23.00** (نفس الـ Product ID بالاسمين لازم يتطابق مع بعض تقريباً، RevenueCat بيوحّدهم).
4. افتح حساب مجاني على [RevenueCat](https://app.revenuecat.com)، اربطه بمشروعي App Store Connect وPlay Console، وأنشئ **Entitlement** اسمه `premium` (نفس القيمة يلي بملف `subscription_constants.dart`) واربطه بالمنتجين.
5. اعمل **Offering** بالـ RevenueCat Dashboard وحط فيه الباكدجين (Monthly / Annual).
6. خد الـ **API keys** (واحد لأندرويد وواحد لآيفون) من RevenueCat Dashboard > Project Settings > API Keys، وحطهم مكان الـ placeholders بـ [`subscription_constants.dart`](lib/core/constants/subscription_constants.dart).
7. بعد هيك التطبيق بيشتغل عادي بدون أي تعديل كود تاني — `PaywallPage` رح تبلش تعرض الأسعار الحقيقية وزر الشراء رح يشتغل فعلياً.
